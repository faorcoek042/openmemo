node_modules/
dist/
build/
out/
target/
.DS_Store
*.log

# 运行时产物 —— 绝不入库
#
# ⚠️ 这些必须**锚定到仓库根**（前导 `/`）。不带前导斜杠的 `models/` 会匹配**任意层级**
# 同名目录，曾经因此把 `apps/web/src/features/models/` 整个 11 个源文件排除在版本库外，
# 并且连带导致 Tailwind v4 不扫描该目录（它遵守 .gitignore）—— 该目录独有的
# `w-[26rem]` / `z-20` / `grid-cols-[…]` 从未被生成，量化下拉因此塌成窄条并被后面的卡片盖住。
# 一条忽略规则同时造成"代码没进库"和"样式没生成"两种毫不相干的症状，且两者都不报错。
/models/
/bin/runtime/
*.gguf
*.bin
*.onnx
*.safetensors
data/
*.sqlite
*.sqlite-wal
*.sqlite-shm
media-cache/

.env
.env.local

# TypeScript / 构建缓存（T-011）
*.tsbuildinfo
.tsbuildinfo/
dist-types/

# 原生组件（whisper.cpp / sherpa-onnx / sqlite-vec / libsimple）的 CMake 构建目录
.build/

# 工具缓存
.pnpm-store/
.eslintcache
.vite/
.cache/
coverage/

# 许可证报告产物（scripts/license-report.mjs 生成，可随时重跑，不入库）
/license-report.json
/license-report.md

# 注意：vendor/<submodule> 由 .gitmodules 管理，不要在此忽略。
# vendor/manifests/*.json 必须入库（ADR-001 强制配套 1：C 类可追溯性）。

# ── 密钥与运行时状态：任何层级都不许进版本库 ────────────────────────
#
# 这几个是**文件名**不是目录名，所以不带前导斜杠是刻意的 ——
# 我们要它们在任何层级都被挡住（用户可能把 dataDir 指到仓库里的任意子目录）。
# 与 `models/` 那次事故不同：那次是**目录名**不带斜杠，误伤了
# `apps/web/src/features/models/`（见 scripts/check-tracked-sources.mjs）。
#
# 加规则前已核实：仓库里没有任何已跟踪的同名文件，`apps/` `packages/` 下也没有。
secrets.json
runtime.json
datadir.json
