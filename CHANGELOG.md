# 版本记录

规则见 `docs/design/D-12-versioning.md`。三句话版本：

- 版本号形如 **`0.N.P`**。**N = 第几个可用的东西**，P = 针对已经交给你的那个 N 的修补。
- **递增 = 交付**。有东西交到用户手上才 +1；光提交代码不算（那是 commit 号的活）。
- 这个文件的**最上面一条必须等于**根 `package.json` 的 `version`，`pnpm check:version` 守着。
  两者由 `pnpm version:bump` 一次写入，所以不存在"改了一个忘了另一个"。

版本号回答的是「**第几个**」。「跑的是哪一份代码」看界面右下角的 commit，
「到底重启了没有」看它旁边的启动时刻 —— 三个问题，三个信号，谁也替不了谁。

---

## 0.7.6 — 2026-09-06

- 🔴 **安全：搜索结果摘要存在存储型 XSS，已修。** `SearchPage` 用 `dangerouslySetInnerHTML` 渲染摘要，而那段注释声称它是服务端 `simple_highlight` 的产物、「不是用户输入」——**两句都是假的**：全仓零 `simple_highlight` 调用，那一格发的就是笔记标题/正文原文。一条标题为 `<img src=x onerror=…>` 的笔记在搜索页上会执行。修法是把转义搬到服务端并定序（**先整体转义、后把哨兵控制字符换成 `<mark>`**），并在旧构建上正反双向验证过（旧的会弹、新的不弹且高亮仍在）
- **正文里的时间锚点此前进不了搜索索引，已修，并带一次性回填。** 服务端推导 `body_text` 时只收 `text` 节点，时间锚点（`[12:34]`）贡献为零 ⇒ **用户照着屏幕上的字搜，搜不到自己刚写的锚点，而且没有任何报错**。⚠️ **升级后回填会在启动时自动跑，存量笔记不需要手工重存**：幂等、游标分页、**不改 `updated_at`**，指纹按投影器行为哈希 ⇒ 投影逻辑变了自动重跑。搜索结果同时改成显示**命中处的上下文**（此前标题非空时永远拿标题当摘要 ⇒ 卡片上标题重复两遍、匹配到的字一个都不出现）
- 界面 / 产品：思维导图的失败路径不再把「读不出来」说成「本来就没有」（判别式 + 500 `MINDMAP_UNREADABLE`，「确实没有导图」仍是 404）；`backends` 面此前**结构上永远标不上**（与已修的 `runtime` 面同型），顶栏「已接通/模拟」计数不再把伪 surface 算进去；时间码格式化**四份合一**收进 `packages/shared`；「共享层」里 39% 其实只有单个 feature 在用 ⇒ 归位，应用外壳三件移出 `components/common`；跨包重复的常量/类型收敛（采样率**四份**、`SEGMENT_FLAG` 三份、`JobState`、`LLM_PURPOSES`、`BreakerVerdict`、`MaskedSecret`）并加了一道**现算**的重复声明门禁
- 🔴 **CI / 守卫（这一版的主体）：`lint-workflows` 那句「1978 条断言」是 `must()` 的调用次数，不是判据条数 —— 静态规则是 89 条。** 逐条消融：甲 81 有专属坏输入 / 乙 7 被相邻断言吞掉 / 丙 1 空转（已修）。⚠️ 那条空转的报错信息写着「这条是空的」，而**空串正是它唯一进不去的分支**。`e2e-import` / `e2e-notes` 两条腿从「结构上不可测」变成可测（判据抽成纯函数 + 自检），抽的过程中抓到 **7 条空转**，其中一条**响起来那天会说假话**（env 分支没归一化，Windows 上会把库里的 ffmpeg 报成「借宿主的」）；`e2e-datadir` 的 C4 被产品的类型变更打断了一个月没人知道（`sourceResidue` 从数组变成三态标签联合），而它自称守的「界面说的和实际发生的必须一致」**POSIX 两条腿一次都没验过** —— 现在验了；**8 枚入口守卫地雷**（经软链路径调用时 exit 0、零输出：`import.meta.url` 是 realpath 过的、`argv[1]` 不是）全部换成统一实现并加了**行为判据**，不用豁免名单；macOS 上 `git grep -E` 的 `\b` 退化成字面 `b` ⇒ locale 扫描器零命中（BSD regex 没有这个 GNU 扩展）；孤儿导出门禁按裸名跨全仓匹配，41 个重名导出对它全部隐形
- ⚠️ **`ci.yml` 绿 ≠ Windows/macOS 上测试通过** —— 那两个平台的结论只在 `ci-crossplatform` 里，别从主门禁那盏绿灯上读它
- ⚠️ 订正与边界：`ci-crossplatform` 的跨平台棘轮在 **08-26 / 08-28 / 09-01 / 09-05 四次定时判红，前三次无人处理**，四次全是方向②（「基线里有一条今天过了」），而棘轮印的是「有人把它修好了」—— **实测同一个 commit 上 8 红 4 绿，是 flaky 不是修好**，已改成 `flaky` 隔离并带红绿证据；本轮的覆盖率测量回答的是「判据有没有牙齿」，**不是「判据钉对了没有」** —— 「钉错东西」那一档逐格删除法**结构上看不见**；`--max-glibcxx` / `--max-cxxabi` 两个基线**没有任何断言钉着**。上一版遗留的仍然成立：**删除接口的不变式变了**（200 ⇒ `filesNotRemoved` 非空不再成立，读取方要把两个桶加起来）；**win32 那条「删不掉」分支从未在真实的 Windows 句柄占用上触发过**

## 0.7.5 — 2026-08-24

- 用户可见文案按「删解释、留事实与出路」精简（中文 −41%、英文 −39%）；组件卡片上那 11,884 字构建日志折进默认收起的折叠 —— 折之前先把那个靠正则嗅散文算出来的警告色换成清单里的枚举（旧做法 13 条里 8 条误判，而且全是证据最强的那几条）；英文界面上 62 处补救按钮、`/models` 的「无法识别的残留」、`/runtime` 的全角冒号不再漏中文；顶栏构建戳只留版本号（`+dirty` 与 commit 移进诊断页，tooltip 里的「启动于」保留）；4 个后端包的「没在这个平台上端到端验过」露到卡片正面，`requiresDriver` 为空时明说「我们不知道」；卸载残留的件数回来了。随包 LICENSE 那句「本包不包含 ffmpeg」订正为三平台都成立的「不包含任何 GPL 组件」，实际内置什么以按平台生成的 THIRD-PARTY-NOTICES 为准（核过是陈述错误、不是义务违反）；`ci-crossplatform` 与 `proxy-coverage` 红了 12 天没人知道 —— 探针那两处截断堵上、按平台收窄、加上两头都断的基线棘轮。⚠️ 删除接口的不变式变了：返回 200 不再意味着「删不掉的清单」非空，读取方必须把「我们主动拒绝」与失败明细两个桶加起来
- ⚠️ 补一句发布当天的实况，免得上面那句被读宽：`ci-crossplatform` 的**三平台全绿只出现过一次，在合并前的分支头上**（run 32662600443）；发布的这个 commit（`b6c4018`）上 macOS 那一格是**红的** —— 棘轮的**第二个方向**按设计响了（「基线里有一条今天居然过了，去划掉」），**不是新伤**。我们选择如实说，而不是先把基线划掉再发。同样的话逐字写在 v0.7.5 发布说明的已知边界第 18 条。另：`ci.yml` 绿 ≠ Windows/macOS 上测试通过，那两个平台只看 `ci-crossplatform`。

## 0.7.4 — 2026-08-17

- 卸载时真正的删除失败不再静默（真删掉 / 我们主动拒绝 / 想删删不动，三档各说各的话，最后一档还说得出能做什么）；五颗禁用按钮的理由从「悬停才有的提示」变成屏幕上看得见的一句话；变异证明只认「断言按设计判红」——超时与页面崩溃不再冒充「如期变红」，凭证里的 mutations 也收成三态

## 0.7.3 — 2026-08-15

- 删除路径的越界检查落到解析层，并让「拒绝」说得出为什么；英文界面上那些半句中文（daemon 不再拼句子）；镜像探测与媒体导入补上带退避的重试；卸载判据改成**逐文件核对盘上还在不在**，并与产品自报的「哪些没删掉」双向对账。⚠️ 前后端契约版本 2→3：升级时一直开着的旧标签页会停止实时更新（刷新即可），而界面上不会说明原因

## 0.7.2 — 2026-08-13

- 关掉 v0.7.1 公开承认的一批已知边界（转写失败不再让笔记永久停在「处理中」、「重新转写」禁用时的理由任何输入方式都读得到、虚拟机里的显示适配器改成三态、更新检测收成六态判别联合）；修掉**流水线任务（转写 / 导图）**的进度条一路显示 100% 的假进度（**下载类任务不受影响** —— 那条最常被盯着看的进度条一直是对的，这也是它活了十天的原因）；英文界面下组件页第一次说英文；补上闸门自己那个「failure 的腿也能发出合格凭证」的洞

## 0.7.1 — 2026-08-12

- 修掉用户在 v0.7.0 Windows 上报的四条（取消下载后的幽灵任务 / 假的「未检测到可用 GPU」/ 已内置 ffmpeg 仍被当成可安装 / Vulkan 后端装不上）；发布凭证的未决计数六条腿全部接线；「重新转写」判据真去解析一次并自愈搬过家的库

## 0.7.0 — 2026-08-10

**第 7 个可用版本。**

- 模型重装会删掉自己刚装的文件、已用空间算错、显卡加速探测报了假话、任务名不跟界面语言走 —— 一批用户能摸到的 bug；同时新增了更新包解压后的 Ed25519 签名校验

## 0.6.0 — 2026-08-09

**第 6 个可用版本。**

- 每个 release 自带它清单指向的全部依赖，不再跨 release 引用

## 0.5.0 — 2026-08-09

**第 5 个可用版本。**

- 卡在「正在校验完整性」其实是界面停在过期事件上、Windows 搬数据目录每次都留旧的一份、GPU 加速装不上、5 个模型在 Windows 上被误判成 CPU 不支持

## 0.4.0 — 2026-08-09

**第 4 个可用版本。**

- 组件目录随包出厂（此前双击打开是空的，装不了任何东西）、包内探针不再依赖启动器、CPU 基线引擎进包、点按钮不再没反应

## 0.3.0 — 2026-08-08

**第 3 个可用版本。**

- 修掉 v0.2.0 装不上/打不开的全部原因：Windows 双击报错、macOS Gatekeeper 拦住、代理到不了 yt-dlp、装了加速后端却选不了

## 0.2.0 — 2026-08-08

**第 2 个可用版本。这是第一个版本号真的有含义的版本。**

- 版本号从"两个互不相干的手写字面量"改成单一事实来源：
  根 `package.json` 的 `version` 是唯一权威，构建时烘焙进 daemon 产物，界面直接显示它。
- 此前界面上那个 `daemon v0.1.0` 来自 `apps/daemon/src/main.ts` 里写死的一行
  `export const VERSION = '0.1.0'`，而各 `package.json` 写的是 `0.0.0`：
  **两个数谁都不知道对方存在，所以从项目开始就没有任何东西会让它们变。**
- 工作区其余 9 个 `package.json` 的 `version` 字段**已删除**（不是同步成一样的值）——
  没有那个字段，就不可能和权威值分叉。
- 新增 `pnpm check:version` 守卫，已接进 `pnpm test:ci-scripts`（CI 门禁）。

## 0.1.0 — （无日期）

**不是一次发布，是一个从没动过的字面量。**

它在 `main.ts` 里被手写下来之后再没被改过，因此它不代表某一次交付，
只能代表"0.2.0 之前的一切"。列在这里是为了让 `0.2.0` 的"第 2 个"有个起点，
而不是假装当初发生过一次版本发布。
