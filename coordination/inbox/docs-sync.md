# inbox: docs-sync

## [2026-08-08 13:20] T-对外文档与现实对齐 DONE

交付: `README.md` · `docs/DEPLOYMENT.md` · `docs/SECURITY.md` · 本回执

### 查出 11 句今天已经不为真的话（逐条，附实测）

**这是本轮的主要产出。** 每条都按 `PROTOCOL §13` 就地订正：原文删除线保留 +
写清何时/被谁/依据什么。**没有一句是静悄悄删掉的。**

| #   | 位置                | 原文（已删除线保留）                                     | 今天的事实                                                                       | 依据                             |
| --- | ------------------- | -------------------------------------------------------- | -------------------------------------------------------------------------------- | -------------------------------- |
| 1   | `README` 许可证     | 「无 LICENSE 文件」                                      | **根目录有 `LICENSE`**（2067 B，发公开包时补的）                                 | `[实测 ls]`                      |
| 2   | `README` 许可证     | 「一旦要分发就是硬阻断」                                 | **已经在公开分发**；GPL 不触发（D-17 §1 三层论证）                               | 已发 v0.2.0 / v0.3.0             |
| 3   | `DEPLOYMENT` §1.1   | 「跑 daemon 需要 Node ≥22 + pnpm 10.15.0」               | **走预编译包一个都不需要**（自带 `runtime/node`）                                | `scripts/build-bundle.mjs`       |
| 4   | `DEPLOYMENT` §1.3   | 「`openmemo-probe` 至今没有分发通道；grep → **0 命中**」 | **今天 10 命中** —— 探针随每个 whisper 包出厂（T-167，2026-08-07）               | `[实测 grep]`                    |
| 5   | `DEPLOYMENT` §1.2②  | 「vulkan-linux 最高 GLIBC **2.38** → 22.04/Debian12 失败」 | **已重建**：glibc 2.35 容器，23 个 ELF 全部 ≤ **2.34**                           | `8cb3b35` / T-161+T-163          |
| 6   | `DEPLOYMENT` §1.2②  | 「那个 vulkan 包**不在** `backends.json` 里，你装不到它」 | **在**。目录 14 条里有它                                                         | `[实测 backends.json]`           |
| 7   | `DEPLOYMENT` §1.3   | 「Windows Vulkan 那条…**未接入**」                       | `whispercpp-vulkan-win-x64` **已在目录**                                         | 同上                             |
| 8   | `DEPLOYMENT` §8.8   | 「Vulkan/CUDA 增量包**都没有接进目录**」                 | **三个都进了**（vulkan-linux / vulkan-win / cuda-win）                           | 同上                             |
| 9   | `DEPLOYMENT` §1.4F1 | 「没有人独立复核，**CI 也不跑它**」                      | `e2e-import` 腿**三平台每次真跑**                                                | `run 31258300507`                |
| 10  | `SECURITY` §4       | 「macOS ad-hoc 签名 **+ 下载后清除 quarantine**」        | **那段代码从来没存在过**，今天仍是零；改为**教用户右键→打开**                    | `[实测 grep]` + ADR-003 §7/§9    |
| 11  | `SECURITY` §4       | 「改为公开分发时**必须**购买证书」                       | **已公开分发且没买**；代价是用户首次手动放行一次，不是"不能发"                   | v0.3.0 已发                      |

**另有 1 处陈旧（不算错，已标注）**：`DEPLOYMENT` §1.3 的来源写 `run 31076010999`，
而 `cold-start-audit` 最近一次成功是 **`run 31167151669`**（README 引的是新的那个）
—— 同一个事实在两份文档里引了两个 run，已在 §1.5 用当前证据补齐。

> **10 与 4 是同一个形状，也正是 §13 收进协议的那个**：
> 结论早就产生了（ADR-003 §7 订正过 quarantine；T-167 给了探针分发通道），
> **但产生结论的那份原文没有被回去改**，于是同一句假话在另一份文档里继续活着。
> 我这次是**从另一头**撞到它的：先读代码求证，才发现 `SECURITY.md` 还在重复
> 一句 `ADR-003` 半个月前就已经订正掉的话。

### `DEPLOYMENT.md` 里预编译包那条路缺了什么

它是**预编译包出现之前**写的，缺的不是细节，是**整条路**：

1. **§1.1 只有"源码构建"一条路** → 已拆成**路线 A（预编译包，不需要 Node/pnpm/git）**
   与**路线 B（克隆源码）**，原表完整保留为路线 B。
2. **完全没有"下载了包、双击打不开"的故障排查** —— 而那是 v0.2.0 最主要的翻车原因。
   → 新增 **§8.9**，三平台各一节，含 macOS Gatekeeper 拦在哪一步的实测原话
   （`spctl` → `rejected / source=no usable signature`）、Windows cp936 那行
   `'m' is not recognized…`、Linux 双击 `.sh` 只会用编辑器打开。
3. **没有"CI 今天到底证明了什么"** → 新增 **§1.5**：四条 e2e 腿 + `bundle-launch-sim`
   + 发布闸门 + 每次 push 的门禁清单，**并明写它们证明不了什么**（真 GPU 那格仍 `UNKNOWN`）。
4. **§3.3 没写 Windows 上「移动」可能是「复制了一份，源还在」** —— 这条有安全后果
   （旧位置留着明文 `secrets.json`）→ 已补，含"你要做什么"。
5. **附录 A 的许可证段落整段过期** → 新增 **A.0** 订正块（LICENSE 已有、GPL 不触发、
   但 `build-media-tools.sh` 那条一步之遥的红线**仍然成立**，没有粉饰）。

### README 行数

**改前 142 行 → 改后 142 行（零增长）**，但信息量增加：
新增了"CI 证明了什么"的一句话指引，压缩了 macOS quarantine 那段（细节移入 §8.9）。
判据照你给的：README 只留"要下什么、怎么打开、能跑在哪、你该知道的"。

### 刻意**没有**改成"什么都好"的地方（如实留着）

- **Windows + CUDA**：`[未验证]`，而且**我们没有办法验** —— 托管 runner 没有 N 卡。
  已在 §1.3 单独写成「🔴 仍然验不了的」，并明写**「验不了 ≠ 没做」**。
- **真 GPU 上到底加不加速**：`UNKNOWN`。
- **`windows-2022/cuda` 为什么保留**：`UNKNOWN`，追查过没找到记录，**刻意不编理由**。
- **Windows 上 `0o600` 不生效**：README 里那条 🔴 原样保留。
- **不买代码签名证书**：保留，并说清它真实的代价（无法证明二进制未被篡改）。
- **`SECURITY` §3 两个 TOCTOU 缺口**：一个字没动。
- **Windows AMD Vulkan**：虽然进了目录，但**没人在真 AMD 卡上验过**，标 `[未验证]`。

### 纪律自查

- 未碰 `:10000`、`/root/data-memo`、机器级指针；未跑 `pkill`；**未建/改/删任何 release**。
- **边界**：另一路正在改 `HANDOFF.md` + `D-03/D-05/D-09/D-10/D-11` + `pending-rulings.md`，
  还有一路在动 `D-17` 的发布闸门那节。**我这三份文件与他们零重叠**，一个都没碰。
  ⚠️ 我**改了 `docs/SECURITY.md` 与 `docs/DEPLOYMENT.md`**（`docs/**` 归 Manager）——
  依据 §13「谁发现原文不实谁就地改…宁可越界改对，不要守界烂着」，**在此申报，请追认**。
- 按 **§12** 提交：用 `git commit -- <pathspec>`（不 stage 再 commit），
  **提交后**用 `git show --stat` 复核文件列表。

需要 Manager 决策:

1. **追认本轮越界**（改了 `docs/SECURITY.md` / `docs/DEPLOYMENT.md` 两份 Manager 地界的文档）。
2. `DEPLOYMENT` §1.3 的平台矩阵仍挂在 `run 31076010999` 上，而更新的 `run 31167151669`
   已经存在。**我没有替换那张表的数字**（我没有核对过新 run 的每一格），
   只在 §1.5 补了当前证据。要不要重跑一次并把表整体换成新 run 的值，请你定。
3. `README` 平台表写 macOS「≥ 13.3」，而分层表里核心其实是 **11.0**。
   两者不矛盾（13.3 来自 whisper.cpp 那个包），但**对外承诺用哪个数字**是你的地界，我没动。

---

## [2026-08-08 14:10] §1.3 平台矩阵逐格重核 DONE

交付: `docs/DEPLOYMENT.md`（§1.3 整节 + 文件头那条边界）

### 重核方法

拿 `cold-start-audit` **run 31167151669**（README 引的那一轮，2026-08-07 09:44，
三平台全绿，**带 `--transcribe`**）的**三个 job 的原始日志**逐格核。
**没有从目录条数推算任何一格** —— 推算出来的数不是实测值。

### 哪些格换了新证据、哪些保留旧值

**结论先说：本表 6 个数据格全部重核到了新的那一轮（`[A]` = 31167151669），
没有一格是只有旧轮（`[B]` = 31076010999）撑着的。**

| 格子                        | 旧值（`[B]`） | 新值（`[A]`）   | 处理                       |
| --------------------------- | ------------- | --------------- | -------------------------- |
| 分母「目录共几个包」        | 22            | **23**          | 换                         |
| Linux 适用的包              | 5             | **6**           | 换（多了 `vulkan-linux`）  |
| Windows 适用的包            | 5             | 5               | 两轮一致，保留（现由 `[A]` 背书） |
| macOS 适用的包              | 5             | 5               | 同上                       |
| 三平台「产品自己下载并校验」 | 5             | 5               | 同上（同一批 5 个工具）    |
| 三平台「借宿主 PATH 的」     | 0             | 0               | 同上                       |
| Linux 转写耗时              | 2.1s          | 2.1s            | 同上                       |
| Windows 转写耗时            | 3.6s          | **4.0s**        | 换                         |
| macOS 转写耗时              | 106.1s        | **95.2s**       | 换                         |
| 转出文本长度                | 108 字符      | 108 字符        | 同上（同一句肯尼迪就职演说） |
| Linux「后端」               | 只有 CPU      | **CPU + Vulkan** | 换（`vulkan-linux` 真装成功，2.0s） |

**原值全部用删除线保留在表格里**，没有静默替换。

### ⚠️ 重核过程中撞到的新问题：这张表有两格**今天已经追不上目录了**

`[A]` 那一轮跑在 **23 个包**的目录上；**今天目录是 25 个** `[实测 2026-08-08]`。
`474c210` 在那一轮**之后**补进了 `whispercpp-vulkan-win-x64` 与 `whispercpp-metal-macos-arm64`。

于是 **Windows 与 macOS 的「适用的包」两格今天的真值 `[未验证]`**：
分母应是 25，而那两个新包是否被判定为"适用"**没有任何一轮跑过**。

**我没有替这两格填数字。** 从"目录里多了两个包"推出"适用数 +1"是推算，不是实测 ——
而这一整轮任务的起点，正是"把新当成真"这个错误。**要填它得真跑一轮 `cold-start-audit`。**

### 顺带一条值得记的实测

`whispercpp-vulkan-linux-x64` 在**没有 GPU 的 CI runner 上被判定为"适用"并安装成功**。

> **所以「适用」≠「会加速」。** 这一条正好给 §1.3 那个 `UNKNOWN` 加了一层：
> 我们能证明的是"装得上"，**证明不了"真 GPU 上加不加速"** —— 两者差得很远，
> 而表格里"适用的包 N 个"这种数字很容易被读成后者。已在表下明写。

### 另外订正的一处（§13）

文件头写着「转写那条路的证据**全部**来自 CI run 31076010999」——
**今天不止那一轮了**（还有 31167151669 与四条 e2e 腿）。已就地订正，原文删除线保留。

### 边界（另两位在同一棵树上）

- 一位在做 macOS 分档自检：`packages/runtime/src/selfcheck.ts`、
  新目录 `packages/runtime/src/platform/`、`apps/web` 的 i18n 与 `SettingsPage.tsx`。
- 一位在把 e2e 凭证接进另外三条腿：`.github/workflows/`、`scripts/ci/verify-e2e-attestation.mjs`。
- **我本轮只动了 `docs/DEPLOYMENT.md` 一个文件**，与两位零重叠。
  ⚠️ README 的 macOS 承诺（13.3）与分层表**一个字没动**，照你的裁决。

### 门禁

`format:check` 我的文件绿（prettier 两遍到不动点）· `lint-workflows` 绿 ·
`check:orphans` 基线未动。本轮改动是**纯 markdown**，不影响测试基线 1578。

需要 Manager 决策:

1. **要不要为那两格再跑一轮 `cold-start-audit`**（25 个包的目录）。
   跑一轮约 6 分钟 × 3 平台，能把 Windows/macOS 那两格从 `[未验证]` 变成实测。
   我没有自己发起 —— 它会真下载几百 MB，且你可能想和别的验证并成一轮。

---

## [2026-08-08 15:05] 让那两格 `[未验证]` 自己会叫人 + §8 留位 DONE

交付: `docs/DEPLOYMENT.md` · `scripts/ci/check-doc-freshness.mjs`（新）· `package.json`（接进 `test:ci-scripts`）

### ① 重验触发条件写成了什么形状

在那两格旁边加了一张**三列表**，照你给的 ①②③：

- **① 缺的是什么测量**：在**今天这份目录（25 个包）**上，Win/mac 各有几个包判定为"适用"，
  以及 `vulkan-win` / `metal-macos` 是否在其中。
- **② 什么事件发生后该补**（任一）：
  **(a)** 探针进包那条修复落地（它直接改变"适用"是怎么算出来的）；
  **(b)** `vendor/manifests/` 里包的条数再变；
  **(c)** `applicability` 判定规则被改动。
- **③ 补它要跑什么**：`gh workflow run cold-start-audit.yml`，
  **三平台约 6 分钟/平台、真下载几百 MB**，跑完从三个 job 日志里逐平台抄
  「目录共 N 个包，适用于本机 M 个」。

### ② 比"写进文档"更不容易被遗忘的挂法 —— 我做了两层，因为它们防的是不同的人

**判据**：提醒要放在**会触发失效的那只手边上**，不是放在**会被误导的那双眼睛边上**。
文档的读者是**用户**，而让这两格失效的是**改代码的人** —— 他不会因为要改探针而来读部署手册。
这也正是 §13 那张表的成因：结论记下了，但**没记在会让它失效的那个人会经过的地方**。

**(b) 已做成机器自己会叫** —— `scripts/ci/check-doc-freshness.mjs`，已进 `pnpm test:ci-scripts`：
把文档里一行**机器可读标记** `<!-- doc-freshness: catalogPacks=25 -->` 与
`vendor/manifests/` 的实际条数对账，不一致当场红，并打印**要跑哪条 workflow、要抄哪几个数**。

> ⚠️ **它不是"这格没测就红"。** 没测在今天是事实不是故障；那样只会变成一条常态红、
> 两周内被所有人学会忽略的守卫（`check-bundle-macos-floors` 已经吃过这一课，最后换成分层）。
> **它红的条件是「你刚刚改动了让这些数字失效的那个东西」** —— 红给改动的人看。

**(a)(c) 测不到**（探针进包的形状还没落地、判定规则改动没有稳定机器判据）→ 挂在**人**那侧：
我在 §1.3 里写好了**一行可直接抄进"探针进包"那条修复验收条件的话**：

> **`docs/DEPLOYMENT.md` §1.3 的 Win/mac「适用的包」两格是在"探针不在包里"的前提下测的。
> 本修复落地后必须重跑一轮 `cold-start-audit.yml` 并更新那两格 ——
> 否则它们会变成第三个"看起来是实测、其实已经不对"的数字。**

⚠️ **这一行需要你放进那条任务的验收条件里**（我不写别人的任务卡）。
**这是本轮唯一还靠人的一环**，也是我认为最有效的一环。

### ③ 反向验证（`/tmp` 隔离副本，PROTOCOL §10，共享树无中间态）

| 组         | 变异                       | 结果                                             |
| ---------- | -------------------------- | ------------------------------------------------ |
| 对照组     | 无                         | 绿 `EXIT=0`                                      |
| **变异 A** | 目录加一个包（25→26）      | **红 `EXIT=1`**，并打印完整处置步骤              |
| **变异 B** | 把 `doc-freshness` 标记删掉 | **红 `EXIT=1`**「改写它就等于把这道提醒拆了」    |
| 还原       | —                          | 绿 `EXIT=0`                                      |

**顺带修掉我自己第一版的一个缺陷**：第一版用正则匹配**正文散文**
（`今天目录是 **N** 个`）取声明值，**当场就被 prettier 的重排打断了**。
换成 HTML 注释标记后，prettier 两遍都不动它。
> **一个靠散文措辞维系的守卫，是一个迟早会静默失效的守卫。**

### ④ §8 留的位置（只有结构，没有内容）

新增 **§8.10「三条已知但成因未定的首次使用故障（占位，等实测）」**，
三行表格 = 三条症状 × 状态 `[未验证]` × 谁在查/落地后补什么：

| 症状                     | 落地后要补的                                             |
| ------------------------ | -------------------------------------------------------- |
| 六个后端全报"探针找不到" | 探针装在哪、为什么最小包里没有、用户怎么确认它在了       |
| 空数据目录下装不了组件   | 冷装第一步卡在哪、有没有手工绕法                         |
| 点按钮完全没反应         | 哪些按钮、什么条件下复现、控制台有没有报错               |

**明写了"现在不写处置办法"的理由**：写了就是猜，而**一条猜出来的排查步骤比没有更糟** ——
用户会照着做、做完没用，然后认为已经排除了这个原因（刚在 macOS 的 `tar` 建议上吃过同样的亏）。
并给填表的人留了形状要求：**先给实测原文（报错原话/日志），再给处置**。

### 门禁

`pnpm test:ci-scripts` **EXIT=0**（含新增那条）· `lint-workflows` 1348/14 ·
`check:orphans` 基线未动 · `format:check` 我的文件绿（prettier 两遍到不动点）·
`eslint` 新脚本无输出。本轮无 TS 改动，测试基线 1589 不受影响。

### 边界（四路同树）

- 探针进包（`scripts/build-bundle.mjs`/`build-bundles.yml`/`ADR-015`+`ADR-003`）、
  最小包冷启动（`e2e-coldstart.yml`）、真浏览器（`apps/web`+`e2e-browser.yml`）、
  `models-llm.json` API 面（`apps/web` locale + `ADR-016`/`D-10`）。
- **我本轮碰的是 `docs/DEPLOYMENT.md` · 新建 `scripts/ci/check-doc-freshness.mjs` ·
  `package.json` 一行**。⚠️ **`docs/adr/` 一个字都没动**（与探针那路有重叠，已避开）。
- `package.json` 是共享文件，我只在 `test:ci-scripts` 末尾追加了一条命令，
  `git diff` 确认**仅 1 行改动**。

需要 Manager 决策:

1. **把 ② 里那一行放进"探针进包"那条修复的验收条件**（我不写别人的任务卡）。
   这是本轮唯一还靠人的一环。
2. `cold-start-audit` **暂不跑**（照你的裁决）。触发条件已就位，(b) 会自己叫。

---

## [2026-08-09 10:30] 文档精简（按读者分家）PROGRESS

交付: `README.md` · `docs/DEPLOYMENT.md` · `docs/SECURITY.md` ·
**新建 `docs/design/D-19-user-doc-provenance.md`** · `scripts/ci/check-doc-freshness.mjs`（重指向）

### 行数

| 文件                | 改前     | 改后    | 差       |
| ------------------- | -------- | ------- | -------- |
| `README.md`         | 142      | **113** | **-29**  |
| `docs/DEPLOYMENT.md`| 1089     | **900** | **-189** |
| `docs/SECURITY.md`  | 482      | **397** | **-85**  |
| `HANDOFF.md`        | 736      | 736     | **0（未做，见下）** |
| **新** `D-19`       | —        | **+400** | 接收迁入 |

面向用户的三份合计 **1713 → 1410（-303 行）**；迁走的内容**一行没丢**，全在 D-19。

### 「从哪搬到哪」对照表（你要能核的那张）

| 内容                                   | 从                        | 到（D-19 的节）                    |
| -------------------------------------- | ------------------------- | ---------------------------------- |
| 路线 A/B 的由来 + §13 删除线原文       | `DEPLOYMENT` §1.1.1       | 「1.1.1 路线 A/B 的由来」          |
| Vulkan/GLIBC 那两句订正 + 删除线原文   | `DEPLOYMENT` §1.2②        | 「1.2② Vulkan/GLIBC 的两句订正」   |
| `[A]`/`[B]`/`[C]` 出处图例 + 逐格重核过程 + 触发条件 + 验收清单 | `DEPLOYMENT` §1.3 | 「1.3 平台矩阵的逐格出处与重核过程」 |
| **原表**（含每格出处标注与历史值删除线） | `DEPLOYMENT` §1.3 表格    | 「§1.3 —— 原表」                   |
| 四条 e2e 腿的 run 号清单               | `DEPLOYMENT` §1.5         | 「1.5 CI 证据链」                  |
| 许可证那两句的订正 + 删除线原文        | `DEPLOYMENT` 附录 A.0     | 「附录 A.0 许可证那两句的订正」    |
| macOS `tar` 那条的删除线原文           | `DEPLOYMENT` §8.9         | 「§8.9 —— 删除线原文」             |
| 七层防护的逐层实现细节（L1–L4）        | `SECURITY` §1             | 「`SECURITY` §1 逐层实现细节」     |

**守恒核对**：三份用户文档改前 `~~` 共 **50** 个 → 改后（含 D-19）**50** 个。
中途掉过 1 对（§8.9 改写时），**已找回并补进 D-19**。

⚠️ **`<!-- doc-freshness: catalogPacks=25 -->` 标记随它描述的那套测量一起搬进 D-19**，
`check-doc-freshness.mjs` 的 `doc:` 已同步指向 D-19，并**重新反向验证**：
对照组绿、目录 25→26 当场红。两条守卫（freshness / pending-claims）现均绿。

### 我删的是哪几类"说法"（不是事实）

1. **证据链**：`[CI 实测 run 3126…]`、逐格 `[A]`/`[C]`、`spctl` 的原始输出、阴阳对照怎么做的。
2. **过程叙述**：「本表逐格重核过…能核到的换成新证据」整段方法论说明。
3. **同一件事说两遍**：README 里"第一次启动装组件"讲了两遍（预编译路线一遍、源码路线又一遍），合并成一处。
4. **散文复述表格**：§1.3 表格下面用散文把表格里的数字又说了一遍。
5. **内部机制**：七层防护每层怎么实现的、L2 的 CVE 细节。

### 我判断"会改变用户行动"因而**留在用户文档**的（请核这条线画得对不对）

- **macOS 第一次必须右键→打开**（他一定撞得到，且不知道就以为包坏了）
- **换 `tar` 解压绕不开** （他会去试，试了没用）
- **`xattr -dr` 等于关掉 Gatekeeper 检查**（他要据此决定做不做）
- **不要在 zip 里直接双击 `start.cmd`**（最常见的失败姿势）
- **绑 `0.0.0.0` 后任何人都能读写你的笔记**（他要据此决定绑不绑）
- **Windows 上 `0o600` 不生效 → Key 明文可读**（他要据此决定放不放 Key）
- **macOS 13.3–15.5 上向量检索与流式 ASR 静默失效**（他要据此决定升不升系统）
- **Windows CUDA 验不了 / Vulkan 没在真 GPU 上验过**（有 N 卡的人要知道自己是小白鼠）
- **macOS 转写 95.2s 是正常的**（否则他以为卡死了）
- **数据目录指针是全机器共享的**（他搬家时会撞到）
- **§3 两个已知未修缺口 / §4 不签名的代价**（一字未动）

### ⚠️ 没做完的（如实报，不假装）

- **`HANDOFF.md` 736 行一行没动。** 我看了结构（①30秒 / ②怎么跑 / ③架构 / ④分工 /
  ⑤踩过的坑 232 行 / ⑥已知未解决 / ⑦第一步建议），判断**§② 与 §⑤ 的价值密度很高**：
  §② 是仓库特有的上手陷阱（`onlyBuiltDependencies`、`tsc -b` 不产 SPA、`gen-build-info`），
  §⑤ 是产品工程教训（与 PROTOCOL 的 agent 协作条款**不重复**）。
  **压它们要逐条判断"新接手的人还需不需要"，我不想在时间不够时草率下刀** —— 留给下一轮。
- **`SECURITY` §2（93 行）**同类可迁（机制细节），本轮只迁了 §1。

### 拿不准、留给你判的

1. **`SECURITY` §1 的 L1 真实状态**（"构建脚本与动态 import 不在 lint 强制范围内"）
   我按"内部机制"迁进了 D-19。但它**是一条已知未覆盖面** —— 若你认为它属于
   "用户/审计者该看见的缺口"，我搬回 `SECURITY` §3 旁边。
2. **§1.3 表里"产品自己下载并校验 5 个 / 借宿主 0 个"** —— 我把数字压成了一句话
   （"一个都没有借你机器上已有的，所以你不需要预装任何东西"）。
   数字本身在 D-19 原表里。若你认为那两个数字对用户有说服力，我加回表格。
3. **README 没有列 D-19** —— 「更多文档」表没加它（避免 README 再长）。
   D-19 是开发记录，用户不需要；但若你要它可发现，我加一行。

### 纪律

- 未碰 `:10000` / `/root/data-memo` / 机器指针；未 `pkill`；未建改删 release。
- **未碰共享 `package.json`**（另一路在改）。改了 `scripts/ci/check-doc-freshness.mjs` ——
  那是我自己的守卫脚本，且**必须跟着标记一起改**，否则守卫会指向一个不存在的锚点。
- 两条守卫改完都**重新反向验证**过。

---

## [2026-08-09 12:10] 三条裁决执行 + SECURITY §2 迁移 DONE

交付: `docs/SECURITY.md` · `docs/design/D-19-user-doc-provenance.md` · `HANDOFF.md`（+6 行索引）

### 行数与守恒

| 文件           | 上一轮 | 本轮    | 差       |
| -------------- | ------ | ------- | -------- |
| `SECURITY.md`  | 397    | **337** | **-60**  |
| `D-19`         | 399    | **510** | +111     |
| `HANDOFF.md`   | 736    | 741     | +5（只加索引，正文未动） |

**守恒核对**：`SECURITY` + `D-19` 两份合起来，改前 `~~` 共 **50** 个 → 改后 **50** 个，
逐条比对删除线**内容**（不是行数），**丢失 0 条**。
`SECURITY` 自己的删除线从 5 条降到 …→ D-19 收到 14 条（含上一轮从 DEPLOYMENT 迁来的）。

### ① L1 那条已搬回 `SECURITY` §3，§3 现在长这样

按你的判据（**它不是证据链，是防护本身的缺口**）落成**缺口 3**，与另两条**并列**、同一张表格式：

```
## 3. ⚠️ 已知未修复的缺口
   ├─ 缺口 1：DNS Rebinding（TOCTOU）          （原有，未动）
   ├─ 缺口 2：文件路径 TOCTOU                   （原有，未动）
   ├─ 缺口 3：L1 的 lint 强制**不覆盖**构建脚本与动态 import   ← 新
   └─ 3.1 缺口 1 与 2 的共同点                  （标题限定，见下）
```

**缺口 3 的正文**照另两条的字段写全：位置 / 性质（**覆盖面缺口，不是可被远程触发的漏洞**）/
具体机制（`files` 只圈产品源码，`scripts/` 不在其中；**动态 `await import()` 静态规则抓不到**）/
可造成的影响 / 为何当前可接受（**不在用户运行路径上**；处理不可信输入的那条路径**在强制范围内**）/
🚨 必须立即修复的条件 / 修复方案。
开头一句写明**为什么它属于这一节**：「§1 说'子进程只能从唯一出口出去，并由 lint 强制'——
那句话**对产品源码成立，对构建脚本不成立**，你不该以为仓库里每一处 `child_process` 都被挡住了。」

⚠️ **两处连带修改**（避免留下不实表述）：
① §3 开头「以下**两个**缺口 Manager 已裁决不阻塞 v1」→ 改成「下面**缺口 1 与 2** 已裁决…，
**缺口 3 是一条覆盖面缺口**，2026-08-09 从 §1 移来」——原句会把新条目也裹进那次裁决里，而它没被裁过。
② §3.1「**两个**缺口的共同点」→「**缺口 1 与 2** 的共同点」——那一节讲的确实只是两条 TOCTOU。

### ② 「5 个自下载 / 0 个借宿主」没搬回来 —— 但结论确认还在

按你的要求核对了**结论本身**是否被一起搬走了。**没有**，仍在 `DEPLOYMENT` §1.3：

> 三平台都是**产品自己下载并校验那 5 个工具**（…），**一个都没有借你机器上已有的** ——
> 所以你不需要预装任何东西。

（`README` 侧对应的是「**自带 Node 运行时** —— 不需要装 Node、pnpm、git」与
「ffmpeg / whisper / yt-dlp / CUDA 都不用预装」。）**结论留、证据走**，符合裁决。

### ③ D-19 不进 README，但**不是孤儿** —— 从 `HANDOFF.md` §③ 指得到

在 HANDOFF「更细的读 `docs/design/D-01`…」那一行**下面**加了 5 行索引，明写三件事：
D-19 是**面向用户那几份文档的证据链与订正账**；2026-08-09 起 run 号 / 逐格出处 /
测量方法 / `PROTOCOL §13` 的删除线原文**都搬到了那里**；
**"要核对我们凭什么这么说，去 D-19；别以为那些记录被删了。"**

**HANDOFF 正文一字未动**（照你"保持原样"的裁决），只加了这条索引 —— 否则那 500 行没有入口。

### ④ `SECURITY` §2 迁完（93 行 → 一张 5 行结论表）

留下的是**每类挡住了什么**（用户/审计者读得懂的结论）：
路径与文件名（**用户永远不提供文件名**）· 媒体协议白名单（`file:`/`concat:`/`subfile:` 实测挡住）·
RSS/XML（拒 DOCTYPE/ENTITY、enclosure URL 重新校验）· 解压（Zip-Slip / 三种绝对路径写法 /
链接**按目标判定** / 三道上限防 zip bomb / **解压前先校验 SHA256**）· 崩溃隔离（子进程边界）。

迁走的是：ffmpeg 攻击的原始输出、"金丝雀用错扩展名"那条方法论教训、行号、`53/53` 验证脚本、
T-012 的 `exit 134` 取证。

⚠️ **迁运时单独标出了两条不是"证据"的东西**，在 D-19 的迁入说明里显式列了：
① §2.4 那条 `§13` 式订正（「symlink 一律拒绝」→ 实际**按目标判定**，因为一律拒绝会让
官方 whisper.cpp 的 `libwhisper.so` 软链装不上）；
② 由它带出的**代码侧未修债**：`unpack.ts:16-19` 的文件头注释**至今仍写着**
"never created / Rejected outright"，与 `:169-183` 的实现打架。**这条债今天仍然成立。**

### 门禁

`format:check` 全仓绿 · `test:ci-scripts` EXIT=0 · `check:orphans` 基线未动 ·
**两条文档守卫均绿**（`check-doc-freshness` / `check-pending-claims`）。

### 纪律

- 未碰 `:10000` / `/root/data-memo` / 机器指针；未 `pkill`；未建改删 release。
- 未碰另四路的文件（`e2e-browser.*` / `verify-e2e-attestation.mjs` / `packages/downloader` /
  `packages/runtime`）。
- **`DEPLOYMENT` §8 没有动** —— 用户新报的四条（AVX2 探测错 / GPU 栏说谎 / CPU 后端下不下来 /
  多个包卡在校验）**成因未定，按你的要求不写**。§8.10 占位仍在，等它们落地。

需要 Manager 决策:

1. **`unpack.ts:16-19` 的注释与实现打架**（上面 ④②）—— 那是代码侧的债，不在我职权内。
   建议派给动 `packages/downloader` 的那一路顺手修（他们正在那个文件附近）。
