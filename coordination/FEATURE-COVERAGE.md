# 功能覆盖矩阵 —— 找遗漏用（Manager 维护）

> **用户指令（2026-08-02）：功能有效优先，性能靠后。先补全功能、找出遗漏与缺失点，再做决策。**
> 状态：🟢 端到端可用 · 🟡 部件可用但未接通 · 🔴 未实现 · ⚪ 未知待确认 · ✂️ 已裁决不做

## 章程 F1–F5

| 功能 | 子项 | 状态 | 说明 |
|------|------|------|------|
| **F1 链接导入** | **yt-dlp 全链路** | 🟢 | ⚠️ **曾对每个视频都失败**，`gpu-runtime` 修了 2 个 bug 后跑通：`Me at the zoo` 19s → 2 段 |
| | HTTP 直链 | 🟢 | 真实 URL 220.2s → 9 chunk / 45 段 |
| | 播客 RSS | 🟡 | **只解析过 feed，从没从 feed 下载并转写过一集**（与 yt-dlp 同一错误形状） |
| | HLS | 🟡 | 拉流+归一化验证过，**没在 HLS 音频上跑过 ASR** |
| | `DirectHttpSource` 断点续传 | 🔴 | **未实现**。播客几百 MB，断网从头再来 |
| | 浏览器 cookie 下会员内容 | 🔴 | memo.ac 有；`--cookies` 是**任意读文件入口**，需先做安全决策 |
| **F2 本地媒体** | 本地文件转写 | 🟢 | 11.0s → 0.80s，长音频 33.6 分钟 80 chunk |
| | **网页拖拽上传（分块）** | 🟡 | 前端有 UI（mock），daemon 端点未接 |
| **F3 录音转文字** | 流式 ASR 引擎 | 🟢 | sherpa zh-14M 跑通，逐字时间戳 |
| | 两阶段合并（保留用户编辑） | 🟢 | 实测「动能」→用户改「动荡」→重跑后保留 |
| | **浏览器麦克风采集→推流** | 🔴 | T-029 进行中 |
| | **daemon `/ws/recorder` 接线** | 🔴 | T-028 进行中 |
| **F4 思维导图** | 转写稿→LLM→导图 | 🟢 | 真实 38 段 → 12 节点，7.5s，schema 校验通过 |
| | 库无关 schema + 双适配器 | 🟢 | mind-elixir + markmap，不 import 渲染库 |
| | Markdown/OPML/FreeMind 导出 | 🟢 | 往返各 31 节点校验通过 |
| | **SVG / PNG 导出** | 🔴 | **未实现**（需渲染器实例，属前端）—— 遗漏点 |
| | **导图编辑（拖拽/撤销/右键）** | ⚪ | mind-elixir 自带，但**前端未接、未验证** |
| | reduce 阶段语义去重 | 🔴 | 当前只 map+拼接，长稿可能主题碎片化 |
| **F5 笔记管理** | 数据模型（26 表） | 🟢 | 落库验证通过 |
| | 转写稿↔音频时间轴联动 | 🟡 | 前端 mock，`/media` Range 未实现 |
| | **全文搜索（FTS5+中文分词）** | 🟡 | DDL 与扩展验证过，**未端到端验证过一次真实搜索** |
| | **向量检索（sqlite-vec）** | 🟡 | 同上，且**没有 embedding 生成环节** —— 遗漏点 |
| | 标签 / 星标 / 文件夹 | ⚪ | 表存在，前端有 mock UI，**接通未知** |
| | 笔记编辑（TipTap） | ⚪ | **未见任何实现报告** —— 疑似遗漏 |
| | 笔记导出 | 🔴 | **未见任何实现** —— 遗漏点 |

## 章程要求 2.1 / 2.2

| 要求 | 子项 | 状态 | 说明 |
|------|------|------|------|
| **2.1 网页装 GPU 依赖** | 硬件探测 | 🟢 | Linux 实测；mac/Win 未验证 |
| | 后端包下载→安装→自检 | 🟢 | 丢 1 个 .so 即生效，实证闭环 |
| | 运行时管理页 UI | 🟡 | 页面已写，**未在真浏览器点过** |
| | mac/Windows 二进制 | 🔴 | **无 GitHub remote，CI 从未执行** |
| **2.2 网页管模型** | 目录/量化/fit 判定 | 🟢 | 真下 59.7MB 模型，SSE 全序列 |
| | 下载/校验/去重/删除 | 🟢 | Zip-Slip 38/38，409 + remediation |
| | 模型管理页 UI | 🟡 | 同 2.1，**未在真浏览器点过** |

## 跨领域

| 项 | 状态 | 说明 |
|----|------|------|
| **端到端（浏览器→daemon→落库→回显）** | 🔴 | **当前最大缺口**，T-028/T-029 进行中 |
| 设置页（API Key / 语言 / 存储路径） | ⚪ | 未见实现报告 |
| LLM 云 provider（Anthropic/OpenAI） | 🔴 | **一次没跑过**，无 API Key |
| LLM 档 2 探测（Ollama/LM Studio） | 🟡 | 代码就绪，本机没装这两个，**未真跑** |
| i18n 中英双语 | 🟡 | 基础设施 T-029 中；models/runtime 页仍内联中文 |
| 桌面外壳 | ✂️ | ADR-003 后置为可选 |
| 说话人分离 | ✂️ | ADR-011 决策 6，章程未含 |
| 翻译/字幕 | ✂️ | ADR-006 决策 6，章程未含 |

## ⛔ 交付阻塞（`gpu-runtime` 确认，**最高优先级**）

**4 个 ASR 模型全不在模型目录里**，导致要求 2.2 装不了它们、F3 与中文默认引擎**在 UI 上根本交付不了**：
| 缺失模型 | 缺了会怎样 |
|---|---|
| `silero VAD` | **分块设计的地基**，没有它长音频切不了 |
| `sherpa 流式 zh-14M` | **F3 的唯一引擎**，录音转文字直接不可用 |
| `paraformer-zh-small` | ADR-013 定的**中文默认引擎** |
| 标点模型 | 否则**中文全篇无标点** |

→ 已派 `model-mgmt` 补 catalog。**这是代码之外的交付阻塞，不补则 F3 与中文交付不了。**

## 🔴/⚪ 汇总 —— 需要补的功能缺口
1. **端到端未接通**（进行中）
2. **F3 麦克风两端接线**（进行中）
2b. **`selectEngine()` 已实现但 daemon 没接线**
2c. **取消后「续跑」一次没真跑过**
2d. `whisper-server` 常驻模式未用 —— 每 chunk 重载模型，大模型下**像卡死**（功能观感问题，非性能）
2e. 多 GPU 无法选 device
3. **笔记编辑器（TipTap）疑似完全遗漏**
4. **笔记导出未实现**
5. **F4 SVG/PNG 导出未实现**
6. **F4 导图编辑未接前端**
7. **向量检索缺 embedding 生成环节**
8. **全文搜索未端到端验证**
9. 标签/星标/文件夹接通状态未知
10. 设置页状态未知
11. yt-dlp 真实下载全链路未验证
12. 云 LLM provider 一次没跑过
13. mac/Win 二进制（缺 GitHub remote）
