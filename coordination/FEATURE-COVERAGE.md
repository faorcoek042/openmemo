# 功能覆盖矩阵 —— 找遗漏用（Manager 维护）

> **用户指令（2026-08-02）：功能有效优先，性能靠后。先补全功能、找出遗漏与缺失点，再做决策。**
> 状态：🟢 端到端可用 · 🟡 部件可用但未接通 · 🔴 未实现/无入口 · ⚪ 未知 · ✂️ 已裁决不做
> 数据来源：`gpu-runtime`(T-030/031)、`architect`(T-029 审计)、`oss-scout`、`model-mgmt` 的实跑报告

---

## ⛔ 三个交付阻塞（优先级高于一切）

| # | 阻塞 | 后果 | 归属 |
|---|------|------|------|
| **B-1** | **4 个 ASR 模型不在模型目录**（silero VAD / sherpa 流式 zh-14M / paraformer-zh-small / 标点模型） | 要求 2.2 装不了 → **F3 与中文默认引擎在 UI 上交付不了** | `model-mgmt` |
| **B-2** | **daemon 只有 6 个端点**（health/auth/events/status/shutdown/echo）；`/api/notes`、`/api/import`、`/api/search` **不存在**，`/media` 返回 501 | **前端 90% 仍跑 mock，无端到端** | `oss-scout` |
| **B-3** | **设置页没有 API Key 输入框** | 用户无法配置 LLM → **F4 在真实环境根本没法用** | `architect` |

---

## 章程 F1–F5

| 功能 | 子项 | 状态 | 说明 |
|------|------|------|------|
| **F1 链接导入** | yt-dlp 全链路 | 🟢 | ⚠️ 曾对**每个视频都失败**，修 2 个 bug 后通：`Me at the zoo` 19s→2 段 |
| | HTTP 直链 | 🟢 | 220.2s → 9 chunk / 45 段 |
| | 播客 RSS | 🟡 | 只解析 feed，**没下载并转写过一集**（T-031 修复中） |
| | HLS | 🟡 | 拉流验证过，**没在 HLS 音频上跑过 ASR**（T-031 修复中） |
| | `DirectHttpSource` 断点续传 | 🔴 | 播客几百 MB，断网从头再来（T-031 修复中） |
| | 会员内容（cookie） | ⏸️ | **`--cookies` 是任意读文件入口，需用户先做安全决策** |
| **F2 本地媒体** | 本地文件转写 | 🟢 | 11.0s→0.80s；长音频 33.6 分钟 80 chunk |
| | **拖拽上传** | 🔴 | **`onDrop` 是空函数——连 mock 都没有，是个死的装饰**（M-2） |
| | daemon 上传端点 | 🔴 | B-2 |
| **F3 录音转文字** | 流式 ASR 引擎 | 🟢 | ⚠️ 此前**能跑是运气**（sherpa CJS interop bug，已修） |
| | 两阶段合并保留用户编辑 | 🟢 | 后端实测通过 |
| | **转写段落编辑 UI** | 🔴 | **M-4：前端没入口 → 上面那条后端逻辑真实使用中永远走不到** |
| | 浏览器麦克风采集 | 🔴 | 未接 |
| | daemon `/ws/recorder` | 🔴 | 未接 |
| **F4 思维导图** | 转写稿→LLM→导图 | 🟡 | 后端跑通（38 段→12 节点），但受 **B-3** 阻塞，用户配不了 Key |
| | 库无关 schema + 双适配器 | 🟢 | |
| | Markdown/OPML/FreeMind 导出 | 🟢 | 往返各 31 节点校验通过 |
| | **导图编辑（拖拽/右键/撤销）** | 🟢 | **本轮补上**——这是选 mind-elixir 而非 markmap 的全部理由 |
| | **SVG/PNG 导出** | 🟢 | **本轮补上**，用 `exportSvg()`/`exportPng()` 矢量导出，非截屏 |
| | reduce 阶段语义去重 | 🔴 | 长稿可能主题碎片化 |
| **F5 笔记管理** | 数据模型（26 表） | 🟢 | |
| | **全文搜索** | 🟡 | **此前前端零入口**，本轮补完整 UI；daemon `/api/search` 不存在（B-2） |
| | 向量检索 | 🔴 | **没有任何地方生成 embedding，链路是断的** |
| | 时间轴联动 | 🟡 | `/media` 501 |
| | **标签/星标/文件夹** | 🔴 | **前端只读、零写入路径**——星标不能点、标签不能加删、文件夹是静态占位 |
| | **笔记编辑器（TipTap）** | 🔴 | **完全遗漏**，依赖装好了，代码里只有一句占位文字 |
| | **笔记导出** | 🔴 | **零实现零入口**，全仓 grep 无命中 |
| | 笔记删除/重命名/移动 | 🔴 | 无 UI（M-6） |
| | "引用此刻"锚点 | 🔴 | 无 UI（M-7） |

## 要求 2.1 / 2.2

| 要求 | 子项 | 状态 | 说明 |
|------|------|------|------|
| **2.1** | 硬件探测 / 后端下载安装自检 | 🟢 | 丢 1 个 `.so` 即生效，实证闭环 |
| | 运行时管理页 | 🟡 | **未在真浏览器点过**（T-027 进行中） |
| | mac/Windows 二进制 | 🔴 | **无 GitHub remote，CI 从未执行** |
| **2.2** | 目录/量化/fit/下载/校验/删除 | 🟢 | 真下 59.7MB，Zip-Slip 38/38 |
| | 模型管理页 | 🟡 | 同上 |
| | **模型目录缺 4 个 ASR 模型** | 🔴 | **B-1** |
| **两者共同** | **首启引导 `/onboarding`** | 🔴 | **M-3：新用户打开一片空白，没人告诉他要先装模型——"用户不碰命令行"在第一步就断了** |

## 跨领域

| 项 | 状态 | 说明 |
|----|------|------|
| 端到端 | 🔴 | **B-2**。前端已做成"按面自动切换"：端点一上线自动切真，无需改代码 |
| 设置页 | 🟡 | 语言/主题/明文告知 ✅；**API Key 输入框 🔴（B-3）** |
| 任务中心 | 🟡 | **只有内存态，刷新即空**，与"关页面任务继续"承诺矛盾（M-5） |
| `/diagnostics` 安全模式页 | 🔴 | M-8 |
| LLM 云 provider | 🔴 | **一次没跑过**（无 Key） |
| LLM 档 2 探测（Ollama/LM Studio） | 🟡 | 本机没装这两个，未真跑 |
| i18n 中英双语 | 🟡 | models/runtime 页仍内联中文 |
| 桌面外壳 / 说话人分离 / 翻译字幕 | ✂️ | 已裁决不做 |

---

## 汇总：待补功能清单（按我的优先级排序）

**第一梯队 —— 不补则功能不成立**
1. B-2 daemon 业务端点（`/api/notes` `/api/import` `/api/search` `/media`）
2. B-1 模型目录补 4 个 ASR 模型
3. B-3 设置页 API Key 输入框
4. M-3 首启引导（要求 2.1/2.2 的第一步）
5. M-4 转写段落编辑 UI（让已跑通的后端逻辑可达）

**第二梯队 —— 章程明确要求但缺失**
6. TipTap 笔记编辑器
7. 笔记导出
8. 标签/星标/文件夹的写入路径
9. M-2 拖拽上传 `onDrop`
10. F3 麦克风两端接线

**第三梯队 —— 功能完整性**
11. M-5 任务中心持久化 · 12. 向量检索 embedding 生成 · 13. RSS/HLS 全链路
14. `DirectHttpSource` 续传 · 15. M-6/M-7/M-8

**待用户决策**：会员内容 cookie（安全）· mac/Win 二进制（GitHub remote）· 云 LLM Key
