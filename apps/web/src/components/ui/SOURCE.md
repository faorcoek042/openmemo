# shadcn/ui —— C2 豁免目录

> 依据：**ADR-002 决策 2**（唯一一项 C2「禁止复制粘贴源码」豁免）。

## 为什么这个目录是例外

shadcn/ui 的分发模式**就是**把源码复制进你的项目——它没有可依赖的上游 npm 包，
"你拥有这些代码"是它的设计意图。因此 ADR-001 的三分法（submodule / 包管理器 / 运行时下载）
对它都不适用。

## 豁免的两个条件（**必须遵守，否则豁免失效**）

1. **隔离**：所有 shadcn/ui 组件只能放在本目录（`apps/web/src/components/ui/`），
   不得与业务组件混放。业务组件放 `apps/web/src/components/` 的其他子目录。
2. **可追溯**：每次通过 CLI 添加组件后，**必须**在下方表格追加一行，
   记录来源 URL、添加日期、以及当时的 `shadcn` CLI 版本。
   这是维持 C2 立法本意（可追溯上游）的唯一手段。

## 来源与许可证

- 上游：https://github.com/shadcn-ui/ui
- 文档：https://ui.shadcn.com
- 许可证：**MIT**
- 无头组件底层库：**今天没有** —— 见下面的订正。

> ### ⚠️ 订正：这里原先写着"底层依赖：Radix UI Primitives（MIT）…已声明在 `apps/web/package.json`"
>
> **那句话是假的，而且从一开始就是假的。** `[实测]` `pnpm-lock.yaml` 里
> `radix` / `base-ui` 的命中数是 **0**，`apps/web/package.json` 里也没有任何一个 ——
> 本目录**一个组件都没复制过**（见下面的清单），所以它根本没有底层库。
> 这一行描述的是"如果哪天开始用 shadcn，大概会装什么"，却写成了既成事实。
>
> D-05 §0.1 已经把"该行需订正"记了两轮（TL;DR、§0.1、§9 V-4、§10-7 四处都提到它），
> 但订正一直没人做 —— 因为它看起来只是个包名问题。它不是：ADR-002 决策 2 的豁免
> 挂在"**可追溯**"这个条件上，而一份把没装的东西写成装了的清单，正好是可追溯性的反面。
>
> **不把它改写成"底层依赖 Base UI"**，理由是那会再造一个同样的坑：
> 上游默认库刚在 2026-07-03 从 Radix 换成 Base UI（D-05 §0.1 已核实），
> 选哪一条路径至今是 D-05 §10-7 的未决项。写一个我们没装、也还没裁决的包名，
> 只是把假话换了个新版本。**真实情况是"零组件 ⇒ 零底层依赖"**，
> 而它会在第一个组件进来时自动失效 —— 那时 CLI 实际拉了什么，就照实记进下面的表格。
>
> 与本目录无关但常被一起提到的 `class-variance-authority` / `clsx` /
> `tailwind-merge` / `lucide-react` 确实装着，但它们是 `components/common/` 自己
> 在用的普通依赖（ADR-001 B 类），不是 shadcn 带进来的 —— 混在这一行里
> 正是它读起来像真的那一半原因。

## 已复制的组件清单

> **目前为空**，从 T-011 建目录至今没变过 —— 本目录只有这份说明文件。
>
> 这不是"还没来得及"。它是一条**预先承诺**：哪天真要用 shadcn，源码只能落在这里，
> 这样 C2 豁免的边界才是一条画好的线，而不是事后追认。
> 目录空着的成本很低（一份说明文件），换掉的是"某人图省事把复制来的源码
> 散进 `components/common/`"——那会让豁免条件 1（隔离）当场失效。
> ⚠️ 所以**别因为它是空的就把它从分层文档里删掉**：它要守的东西恰恰在它为空的时候最容易被破坏。
>
> 添加者请照抄下面的表头格式追加行。

| 组件 | 来源 URL | shadcn CLI 版本 | 添加日期 | 添加者 |
| ---- | -------- | --------------- | -------- | ------ |
| _（无）_ | | | | |
