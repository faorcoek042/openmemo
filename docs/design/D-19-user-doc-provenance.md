---
id: D-19
author: docs-sync
status: ready
date: 2026-08-09
input: 用户 2026-08-09「对于用户来说很多事实是不必要的，应该把这些迁移到其他开发记录文档中」；PROTOCOL §13
---

## TL;DR（≤ 25 行，Manager 只读这里）

- **这份文件是从面向用户的文档里搬出来的"证据链与订正账"**，不是新写的结论。
  用户 2026-08-09：「对于用户来说很多事实是不必要的，应该把这些迁移到其他开发记录文档中，
  而不是保留在面向用户的说明文档。」
- **判据（Manager 2026-08-09 采纳）**：一条事实的去处，取决于**它会改变谁的行动**。
  会改变**用户下一步做什么** → 留在 `README` / `DEPLOYMENT` / `SECURITY`；
  只会改变**开发者对我们说法的信心** → 搬到这里。
- 搬进来的四类：**① run 号与逐格出处标注**（`[A]`/`[B]`/`[C]`）、**② 测量方法与阴阳对照**、
  **③ 内部机制与判据的由来**、**④ `PROTOCOL §13` 的删除线原文**（可追溯性属于开发记录）。
- ⚠️ **搬走 ≠ 删掉。** 每一块都是**原文整块搬运**（脚本抽取，未重打），
  原位置留指针。对照表见 `coordination/inbox/docs-sync.md`。
- ⚠️ **`<!-- doc-freshness: catalogPacks=N -->` 标记随它描述的那套测量一起搬到本文件**，
  `scripts/ci/check-doc-freshness.mjs` 的 `doc:` 已同步指向这里（改完反向验证过）。
- 未验证/存疑：本文件只做**搬运与归位**，**没有重新测量任何一个数字**；
  每块的实测级别标注沿用原文。
- 对其他 agent 的影响：要引用平台矩阵的**出处**，引这里；要给用户看**结论**，引 `DEPLOYMENT §1.3`。

---

## 为什么把这些搬出去

`DEPLOYMENT.md` 从 634 行涨到 1089 行，涨的大多是**真内容** —— 但"内容是真的"不等于"该放在那儿"。

一个下载了包的人，需要知道**"macOS 第一次要右键→打开"**；
他**不需要**知道我们是在 `run 31248222725` 上用 `spctl` 量到 `source=no usable signature` 的。
后者不会改变他的任何一个动作，只会改变**我们自己**对这句话的信心 —— 那是开发记录的事。

> **诚实不等于全都摊在用户面前。** 该记的一条不少，只是记在他不用读的地方。

---

## 迁自 `docs/DEPLOYMENT.md` 1.1.1 路线 A/B 的由来

> **原位置**：`docs/DEPLOYMENT.md`。**搬运时间**：2026-08-09。**搬运人**：`docs-sync`。
> **原文整块搬运，一字未改。**

#### 1.1.1 订正（2026-08-08，`docs-sync`）：这一节曾经只有"路线 B"

**何时**：2026-08-08。**被谁**：`docs-sync`（agent）。**依据什么**：Meta Manager 本轮
任务书要求「把文档和代码的真实状态同步一遍」，并按 `PROTOCOL §13`
「谁发现原文不实谁就地改」。

~~跑 daemon 本身需要 Node ≥ 22 与 pnpm 10.15.0。~~
**← 这是本节 2026-08-08 之前的全部内容，而它是在预编译包出现之前写的。**
v0.2.0 起产品有了自带运行时的预编译包，对**走路线 A 的用户**：
Node / pnpm / git **一个都不需要**。原表保留为"路线 B"，未删。

> **为什么它会烂**：这一节没有写错任何一个字 —— 它只是**没有跟着产品形态变**。
> 这正是 §13 那张表的同一个形状：**结论产生了（我们发了预编译包），
> 而产生结论的那份原文没有被回去改**，于是下一个读者会以为装它必须先装 Node。

---

## 迁自 `docs/DEPLOYMENT.md` 1.2② Vulkan/GLIBC 的两句订正

> **原位置**：`docs/DEPLOYMENT.md`。**搬运时间**：2026-08-09。**搬运人**：`docs-sync`。
> **原文整块搬运，一字未改。**

#### ⚠️ 订正（2026-08-08，`docs-sync`，依据 `PROTOCOL §13`）：上面两句都已不为真

~~`whispercpp-vulkan-linux-x64` 构建机 ubuntu-24.04，最高 GLIBC 需求 **2.38**，
🔴 Ubuntu 22.04(2.35) / Debian 12(2.36) 上加载失败。~~
~~**今天这条不伤用户**：那个 vulkan 包**不在** `backends.json` 里（另有原因，D-11 §8.4），
所以你装不到它。~~

**两处都在 2026-08-07 变了，而且是往好的方向变**（`8cb3b35`，T-161 + T-163）：

1. **GLIBC 2.38 那条已修**：包在 `scripts/ci/buildbox.Dockerfile` 造的 **glibc 2.35 容器**里重建，
   解开后逐个 ELF 跑 `objdump -T`，**23 个最高 GLIBC_2.34** —— Ubuntu 22.04 / Debian 12 都跑得动。
2. **它现在在目录里了**：`[实测 2026-08-08]` `backends.json` 14 条里含
   `whispercpp-vulkan-linux-x64`（同轮进目录的还有 `whispercpp-vulkan-win-x64`、
   `whispercpp-metal-macos-arm64`）。接进目录的四个前提当时逐条消掉了：
   包自包含（`providesFiles` 23 项含 `whisper-cli`）· GLIBC ≤ 2.34 · 解析器不再按 `readdir` ·
   有了下载地址。

> **所以这一格的结论翻转了**：原文说"你装不到它，所以不伤你"；
> 今天是"**你装得到它，而且它不伤你**"——但**理由完全不同**，
> 不是原文那个理由还成立。**这正是 §13 要防的：结论看起来没变，依据已经换了。**

> 顺带记一条更隐蔽的：`GGML_BACKEND_DL=ON` 下 `dlopen` 失败**不算错误**，
> 症状是"装了加速包但没变快"而没有任何东西报错 —— 展开见 §8.8。

---

## 迁自 `docs/DEPLOYMENT.md` 1.3 平台矩阵的逐格出处与重核过程

> **原位置**：`docs/DEPLOYMENT.md`。**搬运时间**：2026-08-09。**搬运人**：`docs-sync`。
> **原文整块搬运，一字未改。**

> ★ **本表 2026-08-08 逐格重核过（`docs-sync`）。** 每一格都注明出处 ——
> **能核到的换成新证据，核不到的保留旧值并标明它来自哪一轮**。
> 一张混着两轮数据、但每格都注明出处的表是诚实的；
> **一张统一换了 run id 却没有逐格重核的表不是**（Manager 2026-08-08 裁定）。
>
> · `[A]` = `cold-start-audit` **run 31167151669**（2026-08-07 09:44，三平台全绿，
> **带 `--transcribe`**）—— 本轮重核用的那一轮。
> · `[B]` = `cold-start-audit` **run 31076010999**（本表此前的来源）。
> · `[C]` = `cold-start-audit` **run 31261016340**（2026-08-08，三平台，**25 个包的目录**）
> —— 由 `prebuilt` 在「探针进包」那条修复落地时跑，用来补上下面那两个 `[未验证]`。
> **只有「目录里适用的包」这一列换成了 `[C]`**；其余各列本轮没有逐格重核，仍是 `[A]`。
>
> **结果：本表 6 个数据格全部重核到了 `[A]`，没有一格是只有 `[B]` 撑着的。**
> 但**本文件其它章节**（§0、§1.1、§1.4 F2、§8.x）仍然引用 `[B]` 的实测 ——
> 那些事实本轮**没有重核**，它们的 run id 就写在原处，**不要把本表的重核当成整篇都重核过了**。

**逐格重核的结果**（哪些换了、哪些没换）：

- **换了新证据**：分母 **22 → 23**（三平台）；**Linux 适用包 5 → 6**
  （`whispercpp-vulkan-linux-x64` 2026-08-07 进目录，`[A]` 里**真的装成功了**，2.0s）；
  Windows 转写 **3.6s → 4.0s**；macOS 转写 **106.1s → 95.2s**。
- **两轮一致、原值保留**：三平台「产品自己下载并校验的 = **5**」
  （`tool.ffmpeg / ffprobe / whisperCli / whisperVad / ytDlp`）与「借宿主 PATH 的 = **0**」；
  转出文本恒为 **108 字符**（同一句肯尼迪就职演说）；Linux 转写 **2.1s** 未变。

#### ✅ 那两格已经补上了（2026-08-08，`prebuilt`）—— 本节保留过程，别删

> **补的结果**：`[C]` = run 31261016340，三平台**各 6 / 25**。
> `whispercpp-vulkan-win-x64` 与 `whispercpp-metal-macos-arm64` **都被判定为适用，且都装成功了**
> （Windows 2.6s / macOS 12.4s）。也就是说下面表格里那两个 `[未验证]` 现在都有答案：**是**。
>
> 触发它的是下面 ②(a)「探针进包那条修复落地」—— 那条修复已落地（`ADR-015 §7.5`）。
> **本节其余部分一字未动**：它记的是「缺什么测量 / 什么事件让它失效 / 补它要跑什么」，
> 那套结构比这一次的数字更值钱，下一次目录再变时还要用。
>
> ⚠️ 仍然**没有**重核的：本表其余各列（下载数 / 借宿主数 / 后端 / 转写秒数）仍是 `[A]`。
> 尤其 **Windows 那格「只有 CPU」可能已经不准** —— `vulkan-win` 这次装成功了，
> 但「装成功」不等于「后端可用」（要探针枚举到设备）。**那一格没测，就仍然标 `[A]`。**

#### ⚠️（历史）这张表有两格今天已经追不上目录了（`[未验证]`，需要新跑一轮才能填）

<!-- doc-freshness: catalogPacks=25 -->
<!-- ↑ 这行不是注释废话：scripts/ci/check-doc-freshness.mjs 拿它和 vendor/manifests/
     的实际条数对账，对不上就当场红并告诉你要重跑哪条 workflow。改目录条数时它会叫你。 -->

`[A]` 那一轮跑在 **23 个包**的目录上。**今天目录是 25 个** `[实测 2026-08-08]` ——
`474c210` 在那一轮**之后**补进了 `whispercpp-vulkan-win-x64` 与 `whispercpp-metal-macos-arm64`。

于是：

| 格子                | 现状                                                                              |
| ------------------- | --------------------------------------------------------------------------------- |
| Windows「适用的包」 | 表里是 `5 / 23`。**今天分母应是 25**，且 `vulkan-win` 是否判定适用 **`[未验证]`** |
| macOS「适用的包」   | 同上；`metal-macos` 是否判定适用 **`[未验证]`**                                   |

**我没有替这两格填新数字** —— 那需要真跑一轮 `cold-start-audit`，而不是从目录条数推算。
**推算出来的数不是实测值。**

##### 这两格什么时候该去补（`[未验证]` 自带触发条件）

判据来自本仓反复吃的亏：**一个待办如果只是"待办"，它就只会一直待着**
（`D-03` 的"请裁决"挂了 3 个月、`ADR-003` 的 quarantine 烂了半个月）。
所以这里不写"待补"，写**什么事件发生之后它就该被补**：

| 项                       | 内容                                                                                                                                                                                                                                      |
| ------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **① 缺的是什么测量**     | 在**今天这份目录**（25 个包）上，Windows 与 macOS 各有几个包被判定为「适用于本机」，以及 `whispercpp-vulkan-win-x64` / `whispercpp-metal-macos-arm64` 是否在其中                                                                          |
| **② 什么事件发生后该补** | 以下**任一**发生：<br>**(a) 探针进包那条修复落地** —— 它直接改变「适用」是怎么算出来的（L2 门控要探针，见 §1.3 那条订正）；<br>**(b) `vendor/manifests/` 里包的条数再变**（增删任何一个包）；<br>**(c) `applicability` 的判定规则被改动** |
| **③ 补它要跑什么**       | `gh workflow run cold-start-audit.yml`（三平台，**约 6 分钟/平台**；会**真下载几百 MB**并真跑一次转写）。跑完把三个 job 日志里「目录共 N 个包，适用于本机 M 个」逐平台抄进上表，并更新 `[A]` 的 run id 与本节的目录条数                   |

⚠️ **(b) 这一条已经做成机器自己会叫**，不靠人记得：
`scripts/ci/check-doc-freshness.mjs`（跑在 `pnpm test:ci-scripts` 里）把**本文档声明的
目录条数**与 `vendor/manifests/` 的**实际条数**对账，不一致就当场红并打印上面这张表。

> **它不是"这格没测就红"** —— 没测在今天是**事实不是故障**，那样只会变成一条常态红、
> 然后被所有人忽略的守卫。它红的条件是**「你刚刚改动了让这些数字失效的那个东西」**，
> 也就是 (b) 那个事件本身。**让改动的人当场知道，而不是让下一个读者被误导。**
>
> (a) 与 (c) **测不到**（探针进包的形状还没落地、判定规则改动没有稳定的机器判据），
> 所以它们挂在**人**那一侧 —— 见下面这条。

###### 给"探针进包"那条修复的验收清单（一行，请直接抄进它的验收条件）

> **`docs/DEPLOYMENT.md` §1.3 的 Windows / macOS「适用的包」两格是在"探针不在包里"
> 的前提下测的。本修复落地后必须重跑一轮 `cold-start-audit.yml` 并更新那两格
> —— 否则它们会变成第三个"看起来是实测、其实已经不对"的数字。**

**为什么把它挂在那条修复上，而不是只写在本文档里**：
本文档的读者是**用户**，而让这两格失效的是**改代码的人** ——
他不会因为要改探针而来读部署手册。**提醒必须放在会触发失效的那只手边上，
不是放在会被误导的那双眼睛边上。** 这也是 §13 那张表的成因：
结论记在了某个地方，但**不是记在会让它失效的那个人会经过的地方**。

> 顺带一条值得记的实测：`whispercpp-vulkan-linux-x64` 在**没有 GPU 的 CI runner 上
> 被判定为"适用"并安装成功**。所以「适用」≠「会加速」——
> **真 GPU 上到底加不加速，仍然是 `UNKNOWN`**（见下方"仍然验不了的"）。

那 5 个工具是 `ffmpeg` / `ffprobe` / `whisper-cli` / `whisper-vad-speech-segments` / `yt-dlp`。
三个平台转出来的文本一字不差（108 字符，肯尼迪就职演说那句），切分方式都是「VAD（按静音）」。
**中文双字词真的搜得到**（判据不是"扩展文件下下来了"）：三平台 `ext.chineseSearch = ok`，
命中 `用户:1 推特:2 中国:1 服务:2`；`[本机实测]` 同一条在开发机上是 `用户:1 推特:1 中国:1 服务:1`。

---

## 迁自 `docs/DEPLOYMENT.md` 1.5 CI 证据链（run 号与腿的清单）

> **原位置**：`docs/DEPLOYMENT.md`。**搬运时间**：2026-08-09。**搬运人**：`docs-sync`。
> **原文整块搬运，一字未改。**

### 1.5 CI 今天真的证明了什么（2026-08-08 新增）

判据一直是**「屏蔽宿主 PATH 的干净机器上真的转出非空文本」**。在此之上，
2026-08-08 起 F1–F5 各有一条**端到端腿**：它们**用预编译包**、走**真实 HTTP**，
三平台各跑一次，而且**每条断言都配了变异证明**（把被测性质拿掉必须当场红）。

| 腿                  | 覆盖                                                                 | 最近一次成功      |
| ------------------- | -------------------------------------------------------------------- | ----------------- |
| `e2e-import`        | F1 链接导入 + F2 本地媒体导入（含 Range 可播）                       | `run 31258300507` |
| `e2e-record`        | F3 录音 → 转写 → 时间轴                                              | `run 31257111284` |
| `e2e-notes`         | F4 思维导图 + F5 笔记检索                                            | `run 31257113778` |
| `e2e-runtime`       | 章程要求 2.1 / 2.2（硬件检测、装后端）                               | `run 31257160728` |
| `bundle-launch-sim` | **人能不能双击打开**（下载标记传播 / Gatekeeper / 启动器真的被执行） | `run 31248222725` |

**发布闸门**：一批包**没跑过这四条 e2e 腿就发不出去**（凭证落在 artifact 名字上，
upload 步骤 `needs` 它）—— 把"靠人记得"换成了机器闸门。

**每次 push 都跑的门禁**（`ci.yml`）：`format:check` · `build:safe` · `typecheck` ·
`lint` · `check:sources` · **`check:orphans`（零引用导出棘轮）** · `test:ci-scripts` ·
`pnpm -r test`。

> ⚠️ **这些都不能证明的那一格**：托管 runner 没有 N 卡，
> 所以「真 GPU 上到底加不加速」**仍然 `UNKNOWN`**（§1.3）。
> 腿多不等于覆盖全 —— 这张表是"证明了什么"，不是"什么都证明了"。

---

---

## 迁自 `docs/DEPLOYMENT.md` 附录 A.0 许可证那两句的订正

> **原位置**：`docs/DEPLOYMENT.md`。**搬运时间**：2026-08-09。**搬运人**：`docs-sync`。
> **原文整块搬运，一字未改。**

### A.0 订正（2026-08-08，`docs-sync`）：这一段有两句已经不为真

**何时**：2026-08-08。**被谁**：`docs-sync`（agent）。**依据什么**：`PROTOCOL §13`
（谁发现原文不实谁就地改）+ 本轮任务书。原文保留在下方删除线里。

~~`package.json` 写的是 **`UNLICENSED`**，且根目录**没有 LICENSE 文件** `[本机实测 ls]`。~~
~~**要分发它，先读 ADR-002 的「升级路径」** —— 主要工作是自建 LGPL 版 FFmpeg，
以及把 yt-dlp 改回可选插件。~~

**今天的真实状态**：

1. **根目录有 `LICENSE` 文件** `[实测 2026-08-08 ls]`（2067 字节，发公开预编译包时补的）。
   它**说明授权状态**，并不改变 `package.json` 的 `UNLICENSED` —— 两者不矛盾。
   预编译包内另有自动生成的 `THIRD-PARTY-NOTICES`（覆盖随包发出的约 91 个 npm 包）。
2. **"要分发就得先自建 LGPL 版 FFmpeg"已被证伪 —— 我们已经在公开分发了。**
   `D-17 §1` 的三层论证（每层单独就够用）：
   ① 随包发出的 npm 依赖闭包 **268 个包里 0 个 GPL/AGPL/LGPL**；
   ② ffmpeg / yt-dlp 的**字节从不经过我们** —— 由用户机器直连 BtbN / yt-dlp 官方
   GitHub 取，我们的 Release 上没有它们；
   ③ 我们调用它们的方式是 `spawn(绝对路径, argv, {shell:false})` ——
   **独立进程、命令行边界，不是链接**。
   → **发预编译包不触发 GPL 的分发义务。**

> ⚠️ **但有三条一步之遥的红线仍然成立**（`D-17 §1.2`）：
> `scripts/build-media-tools.sh` 会把 GPL 的 ffmpeg 重打包成我们的 pack；
> `mirror-model-blobs.mjs` 的白名单没有许可证闸门；
> 后两者现已各加一道闸门，**但第一条只要有人跑一次再上传，我们当场变成 GPL 二进制的分发者**。

---

## 迁自 `docs/DEPLOYMENT.md` §1.3 —— 原表（含逐格出处标注与历史值）

> **原位置**：`docs/DEPLOYMENT.md` §1.3 的表格。**搬运时间**：2026-08-09。
> 面向用户的那份已简化成结论；**这一份保留每格的 `[A]`/`[C]` 出处与历史值删除线**。

| 平台                                   | 目录里适用的包                         | 产品自己下载并校验的 | 借宿主 PATH 的 | 后端                                            | 真的转出非空文本                         |
| -------------------------------------- | -------------------------------------- | -------------------- | -------------- | ----------------------------------------------- | ---------------------------------------- |
| **Linux x64**                          | **6 / 25** `[C]` ~~6 / 23~~ ~~5 / 22~~ | **5** `[A]`          | **0** `[A]`    | CPU + **Vulkan**（包已进目录且**装成功**）`[A]` | ✅ **2.1s** · 108 字符 `[A]`             |
| **Windows x64**                        | **6 / 25** `[C]` ~~5 / 23~~            | **5** `[A]`          | **0** `[A]`    | 只有 CPU（CUDA 见下）`[A]`                      | ✅ **4.0s** · 108 字符 `[A]` ~~3.6s~~    |
| **macOS arm64**                        | **6 / 25** `[C]` ~~5 / 23~~            | **5** `[A]`          | **0** `[A]`    | CPU + Metal + CoreML **同在核心包里** `[A]`     | ✅ **95.2s** · 108 字符 `[A]` ~~106.1s~~ |
| linux-arm64 / macOS Intel / Linux ROCm | —                                      | —                    | —              | —                                               | ✂ 用户 2026-08-05 明确裁掉，无产物       |

---

## 迁自 `docs/SECURITY.md` §1 —— 七层防护的逐层实现细节

> **原位置**：`docs/SECURITY.md` §1（L1 真实状态 / L2 的 Windows CVE 细节 /
> L3 三个易错点 / L4 实测验证）。**搬运时间**：2026-08-09。**原文整块搬运，一字未改。**
>
> **为什么搬**：这些回答的是"我们怎么做到的、验到什么程度"，
> **不会改变用户的任何一个动作** —— 用户需要知道的是"参数注入被挡住了"以及
> "哪些没被挡住"（后者仍留在 `SECURITY.md` §3/§4）。
>
> ⚠️ 其中 **L1 的真实状态**记着一条真实边界：**构建脚本与动态 import 不在 lint 强制范围内**。
> 它不是用户可操作项，但**是一条已知未覆盖面**，搬运时整块保留。

### L1 的真实状态：**产品源码已由 lint 强制（带白名单）；构建脚本与动态 import 不在强制范围内**

> ✅ **2026-08-06 / T-153 更新**：`eslint.config.js` 现在**真的有**这条 `no-restricted-imports`
> （`files: apps/daemon/src/** + packages/*/src/**`，外加一个 **7 文件白名单**），
> 由 `pnpm lint` 执行，并由 `packages/pipeline/src/subprocess/__tests__/childProcessAllowlist.test.ts`
> **真的 spawn 一次 eslint** 来验证它生效（走 `pnpm -r test` 这条事实上的门禁）。
> **但它的覆盖面小于本节标题曾经声称的那个** —— 三条边界见下面的 ③。
>
> 📝 **此前 L1 那一行写着**「`runner.ts` 是全包**唯一**允许 import `node:child_process` 的模块」。
> 实测（剥掉注释后的全仓静态扫描，22 处命中 → **19 处真实 import**，分布在 19 个文件）**这句话不成立**，
> 而且**当时没有任何机器在执行它**。下面这份记录原样保留 —— 三份文档曾互相引用同一条并不存在的
> 控制，下一个人需要知道**这里改过**，而不是看到一段没有历史的新文字。

**① 例外清单（19 处真实 import，`runner.ts` 只是其中之一）**

| 口径                               | 数量   | 具体位置                                                                                                                                                                                                        |
| ---------------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **`packages/pipeline` 包内**       | **2**  | `subprocess/runner.ts:18`（白名单本身）、**`asr/whisperServer.ts:25`**（`:161` 真的 `spawn(bin, argv, …)`）                                                                                                     |
| **产品代码（排除 scripts/tests）** | **7**  | 上面 2 处 + `apps/daemon/src/main.ts:13`、`apps/daemon/src/bootstrap/tls.ts:19`、`packages/runtime/src/selfTest.ts:19`、`packages/runtime/src/probe/runProbe.ts:19`、`packages/runtime/src/detect/system.ts:13` |
| **全仓**                           | **19** | 上面 7 处 + 12 个 `scripts/` 与 `verify-*.mjs`，其中 `packages/downloader/scripts/verify-offline.mjs:640` 是 **`await import('node:child_process')` 动态 import —— 任何静态 lint 规则都抓不到**                 |

（另有 3 处**仅出现在注释里**的字符串：`packages/pipeline/src/index.ts:5`、`runner.ts:3`、`runner.ts:7`。
做这类审计时必须剥注释，否则会数成 22。）

**② 当时（T-153 之前）没有任何 CI / lint 在强制它**

- `eslint.config.js` 里 `no-restricted-imports` 恰好出现 **3 处**（`:64-79`、`:86-97`、`:104`），
  **全是 `apps/web` 的前端分层护栏**（features/A ↮ features/B、lib/components ↮ features），
  **无一处提到 `child_process` 或任何 node 内置模块**。
- `grep -rn "child_process" .github/workflows/ scripts/`（排除 scripts 自身的 import 行）**零输出**。
- `docs/design/D-06-pipeline.md:330-333` 里有一段 `grep -rn "node:child_process" … && exit 1` 的**代码块**，
  但它**只存在于 markdown 里**，没有任何 workflow 或脚本调用它；而且那条 grep 今天跑会直接 FAIL。
- `docs/design/D-01-architecture.md:1061` 更进一步写「CI 用 `no-restricted-imports` 强制，
  `apps/daemon/src/subprocess/**` 之外禁止 import」——**该目录根本不存在**（真实位置是
  `packages/pipeline/src/subprocess/`）。三份文档互相引用同一条**并不存在**的控制，
  形成「看起来被三处证实」的假象。

**③ T-153 之后的准确口径（这一段才是今天的状态）**

| 问题                | 答案                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 规则在哪            | `eslint.config.js`：一个 `files: ['apps/daemon/src/**', 'packages/*/src/**']` 的禁令块 + 一个 **7 文件**的 `off` 白名单块。7 个文件与上表「产品代码」那一行**逐个对应**，性质写在配置旁边                                                                                                                                                                                                                                                                                         |
| 谁在跑              | `pnpm lint`（根 `lint` / `check` 脚本）。另有护栏测试**真的起一次 eslint** 断言四件事：产品代码会报 / 白名单不报 / 前端两条分层护栏没被覆盖掉 / **动态 import 报不了**                                                                                                                                                                                                                                                                                                            |
| 🔴 **它拦不住什么** | **① `await import('node:child_process')`** —— `packages/downloader/scripts/verify-offline.mjs:640` 就是这个形状。静态规则对它无能为力，护栏测试里有一条断言**专门钉住这个盲区**。**② `scripts/**` 与 `verify-*.mjs`**（12 处）**不在范围内**：它们不随产品分发、spawn 的是钉死的命令；一并禁掉只会逼出十几条 `eslint-disable`。**③ `apps/web/**` 不在范围内**：flat config 同名规则是整体覆盖不是合并，圈进来会**悄悄吃掉**两条前端分层护栏（已实测：改成含 web，护栏测试当场红） |
| 所以能怎么说        | ✅「产品源码里 `child_process` 只能出现在这 7 个文件，由 lint 强制」<br>❌「全项目唯一 spawn 出口」—— 这句今天**仍然不成立**（`whisperServer.ts` 那条真债 + `packages/runtime` 的依赖方向）                                                                                                                                                                                                                                                                                       |

**③ 处置**：要么在 `eslint.config.js` 真加一条 `no-restricted-imports`（并把上面 19 处逐个豁免或整改），
要么就把 L1 如实降级为「约定」——本文已按后者措辞。**在加上机器执行者之前，不要再把 L1 当作防线计入。**

### L2 的 Windows 细节（CVE-2024-27980）

Node 在 Windows 上执行 `.bat`/`.cmd` **必须**经过 `cmd.exe`，这会重新引入 shell 元字符解析 ——
这正是 CVE-2024-27980 的成因。既然整个设计建立在 `shell: false` 上，唯一能保住这个承诺的办法
就是**拒绝执行任何需要 shell 的东西**。只允许 `.exe`。

> ⚠️ **实测教训**：这条检查最初**用宿主的 `path.isAbsolute` 判断 Windows 路径**，在 Linux 上
> `isAbsolute('C:\\tools\\x.exe')` 返回 false，导致 Windows 路径一律先被判成穿越，
> **CVE 分支根本不可达 —— 而 `.bat` 的测试一直在"通过"，只是通过的理由是错的**。
> 已修（按目标平台选 `win32.isAbsolute` / `posix.isAbsolute`）。
> 教训写进 ADR-008：**断言拒绝的"错误码"，而不只是断言"被拒绝了"。**

### L3 的三个易错点（都已有对应测试）

1. **控制字符必须在 `new URL()` 之前检查。** `new URL()` 会**静默剥离** `\n`、`\t`、`\r`。
   先 parse 再查，等于把恶意串洗成看起来干净的串。
2. **长度必须按字节算。** `'😀'.repeat(1000)` 是 1000 字符但 **4000 字节**。按 `.length` 判会放行 4KB argv。
3. **我们自己就是 SSRF 的靶子。** daemon 监听本机（**当前默认 `0.0.0.0`**，见 §0），所以
   "让下载器访问 `http://127.0.0.1:<port>/api/v1/settings`" 是针对我们自己 API 的
   confused-deputy 攻击。私网地址过滤里**必须包含回环**。
   （📝 **此前这句写着「daemon 绑在 `127.0.0.1`」** —— 论证本身仍然成立，但**前提句已过期**：
   实际默认绑 `0.0.0.0`，而绑 `0.0.0.0` 只会让这条更重要，不会让它失效。）

### L4 的实测验证（不是声称，是真跑过）

`--ignore-config` 是这一层最重要、也最容易被遗漏的一条：**攻击者只要能写一个 `yt-dlp.conf`，
就能注入 `--exec` 拿到任意命令执行。**

实测（T-025）：

```
planted /root/.config/yt-dlp/config  ->  --exec "touch /tmp/e2e/PWNED"
run YtDlpSource.probe(...)
result: PWNED file created? -> NO ✅ --ignore-config held
```

**这条防护经过真实攻击验证，不是纸面配置。**

---

---

## 迁自 `docs/DEPLOYMENT.md` §8.9 —— macOS 那条订正的删除线原文

> **原位置**：`docs/DEPLOYMENT.md` §8.9（2026-08-09 精简时该节改写为面向用户的版本）。
> 面向用户那份只留「换命令行 tar 也绕不开」这个**结论**；
> **此前写着什么**的删除线原文保留在这里：

~~v0.2.0 的说明里写过「用命令行解压就不会被拦」~~ —— **那句话是错的**。

权威订正记录（含实测表与阴阳对照）在
[`docs/design/D-17-prebuilt-bundles.md`](D-17-prebuilt-bundles.md) §5.1，
那里同样以删除线保留了 D-17 原文的那半句。

---

## 迁自 `docs/SECURITY.md` §2 —— 五类防护的实现细节与取证

> **原位置**：`docs/SECURITY.md` §2（2.1 路径与文件名 / 2.2 媒体协议白名单 /
> 2.3 RSS·XML / 2.4 解压 / 2.5 崩溃隔离）。**搬运时间**：2026-08-09。
> **原文整块搬运，一字未改。**
>
> **为什么搬**：这些回答的是"我们怎么挡住的、验到什么程度、行号在哪"，
> **不会改变用户的任何一个动作**。用户文档里保留的是每一类**挡住了什么**的结论。
>
> ⚠️ **这一块里有两条不是"证据"、需要单独盯着的东西**（搬运时整块保留）：
> ① §2.4 那条 `PROTOCOL §13` 式订正 ——「symlink/hardlink 一律拒绝」曾是原文，
> 实际实现是**按目标判定**（一律拒绝会让官方 whisper.cpp 的 `libwhisper.so` 软链装不上）；
> ② 由 ① 带出的**代码侧未修债**：`unpack.ts:16-19` 的文件头注释**至今仍写着**
> "never created / Rejected outright"，与 `:169-183` 的实现打架。
> **这条债今天仍然成立**，不因为文档搬了家而消失。

## 2. 其它已实施的防护

### 2.1 路径与文件名（D-01 §8.5）

**根本策略：用户永远不提供文件名。** 所有落盘文件名由我们生成，用户提供的标题只存在 DB 的
展示字段里。这从根上消灭了 `../../../.ssh/authorized_keys`、Windows 保留名（`CON`/`PRN`/`COM1`）、
NTFS ADS（`file.txt:evil`）、Unicode 同形字、尾随空格/点等一整族问题。

必须接受路径的少数入口（"从磁盘导入"）走 `assertWithinRoot`：
**先 `realpath` 再比较** —— 只做 `path.resolve` 的词法比较会被 symlink 骗过（有对应测试）。

### 2.2 媒体协议白名单 —— 已用真实攻击验证（T-026）

ffmpeg 不只是转码器，它是一个**协议客户端**，认识 `concat:`、`subfile:`、`file:` 等。
一个精心构造的 HLS 播放列表可以让它**读取本地任意文件并拼进输出**。

**这不是理论风险，实测确认过。** 构造了引用 `file:///tmp/attack/secret.ts` 的恶意播放列表：

```
对照组（允许 file 协议）：
  [in#0] Opening 'file:///tmp/attack/secret.ts' for reading      ← 攻击成立，真的读了本地文件

我们的白名单：
  [file @ …] Protocol 'file' not on whitelist 'http,https,tls,tcp,crypto,httpproxy'!
```

三种变体（`file:` / `concat:` / `subfile:`）全部被挡。

> ⚠️ **测试方法论教训**：第一次用 `.txt` 金丝雀，攻击"失败"了——但挡住它的是 ffmpeg 8.x 自己的
> `allowed_segment_extensions`，**不是我们的白名单**。换成 `.ts` 扩展名才隔离出真正起作用的层。
> 与 ADR-008 记录的"假绿灯"同源：**必须确认"挡住了"是被哪一层挡住的。**

#### ⚠️ 由此查出并修复的一个真实漏洞（本地播放列表导入）

远程路径安全，**本地路径当时不安全**。`LocalFileSource` 的扩展名白名单包含 `.m3u8`，
而本地分支必须传 `-protocol_whitelist file`（否则普通本地媒体解不了）。实测：

```
$ ffmpeg -protocol_whitelist file -i /tmp/attack/local_evil.m3u8 …
[in#0] Opening 'file:///tmp/attack/secret.ts' for reading      ← 越过了受管根目录
```

**协议白名单在这里救不了我们** —— 播放列表是通过一个我们**故意启用**的协议读本地文件，
直接绕过 `assertWithinRoot` 的保证。攻击路径：用户下载/收到恶意 `.m3u8`，当作本地媒体导入。

**已修复**：① 本地导入拒绝一切播放列表扩展名（`.m3u8 .m3u .pls .xspf .asx .wpl`）；
② 双保险——`ffprobe` 的 `format_name` 命中 `hls|applehttp|m3u` 也拒绝（防改名绕过）；
③ `match()` 对播放列表返回 0。**远程 HLS 不受影响**（白名单里没有 `file`，已验证能挡住同一攻击）。

- 处理本地文件：`-protocol_whitelist file`
- 处理远程/HLS：`-protocol_whitelist https,tls,tcp,crypto,httpproxy` —— **不含 `file`**

另外：**用户可控字符串绝不进 `-filter_complex` / `-vf` / `-metadata`**。
filter 语法有自己的转义规则（`:` `,` `[` `]` `'` `\`），是嵌在单个 argv 元素**内部**的第二套注入语法。

### 2.3 RSS / XML

`RssSource` 用小型标签扫描器而非通用 XML 解析器。理由：我们只需要四个字段，而 XML 解析器是
经典的 XXE 载体。**我们不实现实体展开、不取 DTD、不跟随外部引用**，并且**携带 `<!DOCTYPE>` 或
`<!ENTITY>` 的 feed 直接拒绝**。

feed 里的每个 enclosure URL **都要重新过一遍 `validateHttpUrl`** —— 恶意 feed 只是提供
`file:///etc/passwd` 的另一种方式。

### 2.4 解压（GPU 后端包）

实现：`packages/downloader/src/unpack.ts`（742 行）；独立验证脚本
`packages/downloader/scripts/verify-unpack.mjs`（**53/53**，见 ADR-015:44）。

- **Zip-Slip / 绝对路径 / `..` 段**：一律拒绝。绝对路径覆盖 POSIX `/`、Windows `C:\` 与 UNC `\\`
  三种写法（`unpack.ts:131-143`）；`..` 段单独拒（`:148-149`）；resolve 之后**再做一次前缀比对**
  作为纵深防御（`:165`）。
- **链接条目（symlink/hardlink）：按目标判定，不是一律拒绝。**
  绝对目标 → 拒；解析后逃出 `destRoot` → 拒；落在根内 → 放行（`unpack.ts:169-208`）。
  > 📝 **此前这一节写着「symlink/hardlink 条目…一律拒绝」。** 订正原因写在 `unpack.ts:169-183` 的注释里：
  > 「Rejecting EVERY symlink is too blunt and breaks real software」——**官方 whisper.cpp tarball
  > 自带 `libwhisper.so -> libwhisper.so.1.7.6`，一律拒绝会让我们自己的 ASR 后端装不上。**
  > ⚠️ 附带一条代码侧的债（不在本次文档订正范围，但值得同批修）：`unpack.ts:16-19` 的文件头注释
  > **自己也还写着 "never created / Rejected outright"**，与 `:169-183` 的实现打架。
- **资源上限**：条目数上限 `maxEntries`（默认 20000，`:60-61, 96, 104-107`）、未压缩总字节上限
  （默认 4 GiB，`:58-59, 119`）、`zlib.maxOutputLength` 解压中途中止（`:401`）、
  xz 解压中途字节天花板（`:652-657`）——防 zip bomb。
- **解压前先校验整包 SHA256**（`packages/downloader/src/verify.ts` + `install()` 路径；
  ADR-004 决策 5 —— Ollama 的下载器没做校验，我们必须补上）。

### 2.5 崩溃隔离

所有原生工具走**子进程**（ADR-003 决策 1）。这不只是架构洁癖：T-012 实测发现，
whisper.cpp 在后端库缺失时会调 `ggml_abort()` 直接 SIGABRT 退出（exit 134），
**进程内 N-API 绑定会把整个 daemon 一起打死**。

---
